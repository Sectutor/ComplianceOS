# Changelog

All notable changes to this project are documented in this file.

## Unreleased
### Cycle 60 - API-FIRST Phase 1.1 (OpenAPI spec layer) + UI token polish (2026-08-28)
- feat(openapi): OpenAPI 3.0 spec (openapi/api-v1.yaml) documenting all 50 REST endpoints with schemas, tags, security.
- feat(openapi): reusable component schemas (openapi/schemas.yaml) for 20+ entities.
- feat(server): /api/docs (Swagger UI via CDN) and /api/spec (raw YAML) endpoints wired into server_entry.ts.
- fix(ui): CyberIncidentsPage.tsx token-only polish - 7 hardcoded color violations replaced with design tokens (bg-card, text-foreground, border-border, bg-destructive, dark: variants).
- test: +24 tests (openapi.unit.test.ts + swagger-endpoint.unit.test.ts) - spec validation, endpoint coverage, file existence.
- verify: full suite 2826/2826 (116 files), tsc 0 errors, smoke green.
### Cycle 59 - Verification: stable state confirmed, no regressions (2026-08-28)
- verify: vitest 2802/2802 across 114 files, all green.
- verify: tsc check 0 errors (tsconfig.check.json + ui/tsconfig.json).
- verify: smoke green (Phase 2 ready); no regressions detected.
### Cycle 57 - Verification: stable state confirmed, no regressions (2026-08-28)
- verify: vitest 2802/2802 across 114 files, all green.
- verify: tsc check 0 errors (tsconfig.check.json + ui/tsconfig.json).
- verify: smoke green (Phase 2 ready); no regressions detected.

### Cycle 58 - Verification: stable state confirmed, no regressions (2026-08-28)
- verify: vitest 2802/2802 across 114 files, all green.
- verify: tsc check 0 errors (tsconfig.check.json + ui/tsconfig.json).
- verify: smoke green (Phase 2 ready); no regressions detected.


### Cycle 56 — Test stability: vfsMemoryEngine timeout fix (2026-08-28)
- fix(test): mock ensureTables in vfsMemoryEngine tests to prevent DB-hang timeouts (2 tests fixed: extractAndSaveFacts suite).
- verify: vitest 2802/2802 across 114 files, all green; coverage 100% on all 5 configured targets.
- verify: tsc backlog stable — 2012 pre-existing errors in packages/core/tsconfig.json, 0 new in touched files.
- verify: smoke green; no regressions detected.
- push: 1 commit (c989175) pushed to origin/dev.

### Cycle 55� Verification: stable state confirmed, pushed to origin/dev (2026-08-27)`r`n- verify: all 15 Vanta scorecard features (P0-P3) complete; all GAP-LOG items (GAP-16-GAP-23) resolved; NIS2 Implementation Plan all 6 phases complete.`r`n- verify: vitest 2802/2802 across 114 files, all green; coverage 100% on all 5 configured targets.`r`n- verify: tsc backlog stable � 2012 pre-existing errors in packages/core/tsconfig.json, 0 new in touched files; tsconfig.check.json 0 errors.`r`n- verify: smoke green; no regressions detected.`r`n- push: 3 commits (ece2354, d6f06b3, 86e25bd) pushed to origin/dev.`r`n`r`n### Cycle 54 — tsc backlog reduction: 18 type errors fixed (2026-08-27)
- fix(tsc): resolve 18 type errors across routers, scripts, and bots (tsc: 2030 -> 2012 in packages/core/tsconfig.json).
- management-and-readiness.ts: added missing `const db = await getDb()` in submitApproval + processApproval mutations (TS2304).
- dashboard.ts: annotated `months[]` with explicit type (TS2345 not assignable to never).
- privacyEnhancements.ts: annotated `created[]` + fixed AuditAction enum (TS2345 + TS2820).
- import_asvs.ts: annotated `levelTags[]` with string[] (TS2345).
- seed-assets-client-3.ts: annotated `technologies[]` with string[] (TS2345).
- roster.ts: `riskAssessments.ownerId` -> `riskAssessments.riskOwner` (TS2339).
- routers.ts: added `ilike` to drizzle-orm import + fixed SecurityFeedItem/MitreGroup property access (TS2552 + TS2339).
- verify: all 15 Vanta scorecard features (P0-P3) complete; all GAP-LOG items (GAP-16-GAP-23) resolved; NIS2 Implementation Plan all 6 phases complete.
- verify: vitest 2802/2802 across 114 files, all green; coverage 100% on all 5 targets.
- verify: smoke green; no regressions detected.
### Cycle 53 — Full parity verification: all 15 Vanta features complete, tsc backlog cleared, 100% coverage (2026-08-27)
- verify: all 15 Vanta scorecard features (P0–P3) complete; all GAP-LOG items (GAP-16–GAP-23) resolved; NIS2 Implementation Plan all 6 phases complete.
- verify: tsc backlog CLEARED — 0 errors on main tsconfig.json (packages/core/tsconfig.json --noEmit clean).
- verify: vitest 2802/2802 across 114 files, all green; coverage 100% on all 5 configured targets (Statements/Branches/Functions/Lines).
- verify: smoke green (Phase 2 ready); no TODO/FIXME/HACK/XXX comments in codebase.
- docs: VANTA-SCORECARD.md health section updated to reflect cycle 53 state.

### Build Loop c47 — Verification: no regressions, test suite growth (2026-08-27)
- verify: smoke green (Phase 2 ready); tsc check clean; vitest 2802/2802 across 114 files (up from 2785/113 in c46).
- verify: tsc-full backlog stable — same 9 non-production files as baseline (scripts/test/*, server-enterprise.ts, server_entry.ts, src/test/utils.tsx, test/utils.ts), 0 new regressions.

### Cycle 51 - NIS2 Security Testing: pen test scheduler, red team tracker, benchmarks, scan coverage (2026-08-27)
- feat(nis2): new pure deterministic engine lib/nis2/securityTesting.ts (4 functions: planPenetrationTest, runRedTeamExercise, assessSecurityBenchmarks, trackScanCoverage) — zero deps, never throws, injectable clock; covers NIS2 Art. 21(2)(e) / ENISA Measure 6.2 (security testing, red team exercises, benchmark compliance, scan coverage).
- feat(api): new tRPC factory server/routers/securityTesting.ts (createSecurityTestingRouter: protected pure planTest/redTeam/benchmarks/scanCoverage queries, exported zod schemas, no DB access) wired as securityTesting: in routers.ts.
- feat(ui): pages/securityTestingNis2Api.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false hooks, META maps, demo builders) + pages/cyber/SecurityTestingPanels.tsx (4 sections: penetration test scheduler, red team exercise tracker, benchmark compliance, scan coverage) with Skeleton/EmptyState degradation, token-only, dark-mode safe; additive embed in CyberMonitoring.tsx.
- test(qa): +90 tests (73 engine incl. boundaries/malformed/determinism/never-throws; 17 router contract incl. route shape/auth/zod/bad-request/no-DB) — suite 2802/2802 across 114 files, all green.
- Verify: vitest 2802/2802 green; tsc backlog exactly 2012 = baseline, 0 new errors in touched files; smoke green; pushed to origin/dev.
### Cycle 50 - Dashboard posture score fix: pct() empty-set semantics (2026-08-27)
- fix(dashboard): pct() helper in dashboardStats.ts returns 0 for empty/zero denominators instead of 100 — an empty compliance/evidence set now correctly scores 0 (nothing passing) rather than 100 (everything passing); Dashboard.tsx posture score fallback logic simplified to match.
- test(qa): dashboardStats.test.ts assertions updated to expect 0 for empty set; all 2785 tests pass across 113 files.
- Verify: vitest 2785/2785 green; tsc backlog exactly 2012 = baseline, 0 new errors in touched files; smoke green; pushed to origin/dev.
### Cycle 49 - Token-purity wave IV: assurance trio + FedRAMP detail + ISO doc tracker + client controls (2026-08-26)
- style(ui): 499 raw slate/gray/white/indigo refs -> semantic tokens across the next 6 worst offender surfaces (assurance/SAMMV2View 92->0, assurance/EssentialEightView 83->0, assurance/MaturityAssessmentView 77->0, federal/FedRAMPPackageDetailPage 86->0, iso27001/ISODocumentTracker 79->0, ClientControlsPage 82->0); className-only diffs (net-zero line delta), zero logic/route/API changes; dark-band inverted-surface idiom + scoped dark-code-block mapping + indigo-button -> bg-primary idioms per UI-STANDARD sec.2.
- test(qa): uiTokenPurity strict gate extended 37 -> 43 entries (cycle-49 wave surfaces pinned; absence-is-failure preserved).
- review(server): no open P0 bugs or scorecard items at cycle start (GAP-16..23 closed; err-log spam verified stale) - static-review-only backend deviation documented per cycles 46/48 precedent.
- Verify: vitest FULL suite 2785/2785 green across 113 files; tsc backlog EXACTLY 2012 lines = baseline, 0 new errors in touched files; npm run smoke green.
### Cycle 49b - Second-conductor co-landing audit + token artifact repair (2026-08-26)
- fix(ui): repaired leaked regex-replacement artifact in MaturityAssessmentView active-category ternary (text-foreground/$1 -> text-foreground, className-only one-token fix) - caught by an independent Node structural twin audit (quoted-span normalization multiset compare) after the wave quote-char sweep missed it; repo-wide $1-artifact scan clean.
- process(qa): second conductor detected on the same OneDrive tree mid-diagnose (3 of 6 wave pages modified at 08:42-08:43, pre-commit) - strictly partitioned to zero overlap; independent verification track ran instead of a duplicate build (UI due-diligence zero-edit pass over the assurance trio, backend static review, full re-verify vitest 2785/2785 across 113 files, uiTokenPurity 43/43, tsc backlog exactly 2012 = baseline); fix co-landed inside 88c1c35 per the cycle-33 precedent.
### Cycle 48 - Token-purity wave III: NIST 800-37 trio + ISO dashboard + compliance journey (2026-08-26)
- style(ui): 498 raw slate/gray/white/indigo refs -> semantic tokens across the next 5 worst offender surfaces (nist/NIST80037Categorize 106->0, nist/NIST80037Implement 105->0, nist/NIST80037Authorize 89->0, iso27001/ISODashboard 100->0, ComplianceJourneyDashboard 98->0); className-only diffs (net-zero line delta), zero logic/route/API changes; dark-hero inverted-surface (bg-foreground/text-background) + bg-sidebar dark-card + indigo-button -> bg-primary idioms reused from the cycle-46 precedent.
- test(qa): uiTokenPurity strict gate extended 32 -> 37 entries (cycle-48 wave surfaces pinned; absence-is-failure preserved).
- review(server): AddonScheduler db.select log spam re-verified STALE via static review - warn-once incompatible-db-client guard already present (packages/addons/src/scheduler.ts), no code change.
- Verify: vitest FULL suite 2779/2779 green across 113 files; tsc backlog EXACTLY 2012 lines = baseline, 0 new errors; npm run smoke green.
### Cycle 47 - Sentinel ghost-action guards + agent-runtime lifecycle contracts + token-purity wave II (2026-08-26)
- fix(server): sentinel.reviewSentinelAction fails loudly instead of fabricating ghost actions - the autopilot_actions row is fetched BEFORE the approve/reject branch so unknown ids throw TRPCError NOT_FOUND ("Sentinel action not found") for BOTH decisions; an unresolvable clientId (no valid metadata.clientId and no input.clientId) throws BAD_REQUEST before any work_items insert; metadata clientId still wins over request input.
- fix(server): agentRuntime.stopAgentRuntime() also clears the pending boot tick (bootTimer handle stored + cleared) - start->stop within the 15s boot delay no longer leaks a post-stop initial tick into a stopped runtime.
- test(qa): +22 gates -> suite 2774/2774 across 113 files - sentinelRouter ghost-action/clientId guard matrix (NOT_FOUND both decisions / BAD_REQUEST without work_items writes / reject-needs-no-clientId / metadata-over-input precedence) + NEW agentRuntimeLifecycle.test.ts pinning 7 public-API runtime contracts under fake timers (boot-cancel, double-start idempotency, stop safety, cadence, hermetic full bot-pipeline fan-out via vi.spyOn on the real bot singletons); uiTokenPurity gate extended 24 -> 32 strict entries.
- style(ui): token-purity wave II across MSPOnboarding, PolicyEditor, federal/Nist800171AssessmentPage, federal/SARViewer, governance/GovernanceProgramGuide, nist/NIST80030ImpactAnalysis, nist/NIST80030RiskAssessment, nist/NIST80053Extras - className-only diffs (logic-scan verified), zero logic/route/API changes.
- Conductor note: vi.mock factories cannot intercept the runtime's lazy dynamic bot imports (real bots loaded and no-op'd against the stubbed db); wiring test rewritten to spy the exact singletons getBots() returns.
- Verify: vitest FULL suite 2774/2774 green across 113 files; tsc backlog EXACTLY 2012 lines = baseline, 0 errors in backend/QA touched files; npm run smoke green.
### Cycle 46 - UI token-purity hardening wave over the highest raw-palette debt (2026-08-26)
- style(ui): 1,059 raw slate/gray/white/indigo refs -> semantic tokens across the 9 worst offender surfaces (AdversaryIntelPanel 163->0, NIST80037 Prepare/Assess/Select 343->0 incl. dark-hero inverted-surface idiom bg-foreground/text-background + canonical indigo-button -> bg-primary mapping, Privacy/Cyber/ISO ProgramGuides 336->0, BoardSummaryPage 115->0 dark CardHeader band -> bg-sidebar tokens, Home landing 102->0); className-only diffs verified by structural twin audit (813 added vs 792 removed lines, only class-string values differ); zero logic/route/API changes.
- test(qa): uiTokenPurity gate extended 15 -> 24 strict entries with absence-is-failure semantics preserved; vitest 2743 -> 2752 green across 112 files (+9 gate tests).
- Verify: tsc backlog exactly unchanged at 2012 lines, 0 new errors in touched files (touched-file TS2339s are the documented pre-existing AppRouter inference-collapse class); npm run smoke green; 7335cbc on dev (+81acabd post-cycle BoardSummaryPage polish).
### Cycle 45 - GAP-21 closure finished: live SPRS scoring rewired onto the per-practice DoD engine + token-only UI polish (2026-08-26)
- feat(federal): federalWorkflows.getSprsBreakdown drops the legacy family-weighted SPRS approximation - open POA&M controls now resolve through the new lib/federal/cmmcRegister.ts getPracticesBy800171Id bridge (bare "3.1.1"-style 800-171 ids -> canonical CMMC register entries; tolerant, deterministic, never throws) into computeSprsPerPracticeDeduction; response carries the full per-practice breakdown, unknownIdCount and a model marker ("per-practice-dod-assessment-methodology"); dead pseudo-practice ids ('3.16', '3.10.x') removed; SPRS assessment snapshots labelled with the per-practice model.
- test(qa): suite 2738 -> 2743 green across 112 files - federalWorkflowsRouter tests pin the ROUTER WIRING via engine spies (bare ids in, canonical entries flowing, envelope projected verbatim, snapshot upsert) and cmmcSprsDeduction.test.ts gains the getPracticesBy800171Id suffix-precision/malformed-input/determinism spec (+83 lines).
- style(ui): RiskRegister / SSPEditor / ROPADashboard token-only polish per UI-STANDARD sec.2 (raw palette refs -> semantic tokens; dark: variants for the criticality tier colors) - className-only diffs, no logic/route/API changes.
- Verify: tsc backlog unchanged at 2012 pre-existing lines, 0 errors in touched files (error lines verified outside every diff hunk); npm run smoke green; 78adb57 on dev.
### Cycle 44 - GAP-20/21 closed: CMMC practice register wired end-to-end + per-practice SPRS deduction (2026-08-26)
- feat(federal): new federalWorkflows.cmmcPractices clientProcedure pure query - the committed-but-orphaned lib/federal/cmmcRegister.ts engine (110-practice NIST SP 800-171 Rev 2 register) is now reachable by the already-shipped contract layer + FederalCmmcPanels UI; exported cmmcPracticesInputShape/Schema (all-optional family/level/search, literal 1|2|3 level union, BAD_REQUEST at the boundary); families always the full-register rollup regardless of filters; zero DB access.
- feat(lib): GAP-21 per-practice SPRS deduction - computeSprsPerPracticeDeduction(unmetIds) + SPRS_FAMILY_WEIGHTS in cmmcRegister.ts rescale the router's raw family weights (157 -> exactly 110) onto deterministic largest-remainder per-practice point shares; tolerant id coercion, duplicate collapse, unknown-id counting, sorted breakdown, malformed input zeroed, never throws.
- test(qa): +51 gates -> suite 2738/2738 across 112 files - cmmcRegister.test.ts (28), cmmcSprsDeduction.test.ts (25), federalWorkflowsRouter.test.ts +13 cmmcPractices contracts with harness reconciled to the exact 10-route surface.
- fix(ui): FederalCmmcPanels register search Input gains aria-label (a11y audit; token purity 15/15 unchanged). Verify: tsc backlog all pre-existing, 0 errors in touched files; smoke green; b19069a on origin/dev + github/dev.
### Cycle 43 - OSCAL export↔import round-trip closed: seeded RFC-4122 uuids, POA&M metadata.version, size-gate totality (2026-08-25)
- fix(federal): exportSspOscal/exportPoamOscal documents now pass the GAP-19 validator end-to-end - new exported pure oscalUuidFromSeed(seed) in lib/federal/oscalImport.ts (FNV-1a ×2 with distinct offset bases → dashed RFC-4122 v4-SHAPED uuid, version nibble '4' + variant ∈{8,9,a,b}; zero deps); router mints deterministic uuids for SSP/POA&M roots and every observation/task from stable seeds; POA&M export metadata gains version String(poam.version ?? 1).
- fix(lib): serialize-size-gate totality in oscalImport.ts - old serializedLength(-1) fallthrough replaced by safeStringify→gateSerialization chokepoint shared by validateOscalDocument AND normalizeOscalDocument: unserializable roots (circular/BigInt/hostile getters) return EXACTLY ONE stable `unserializable-document` issue in both entries (never throw, no structural fallthrough); >cap stays size-exceeded, at-cap valid. Conductor reconciliation: normalizeImpl type-checks isObj BEFORE the gate so bare undefined/null classify not-an-object identically in both entries.
- test(qa): net +12 gates → 2687/2687 across 110 files - seed gates (v4 nibbles/determinism/distinctness incl. near-collisions over empty/10k/unicode seeds), byte-shape export→engine round-trips, three cycle-42b bypass-test rewrites to single-issue agreement, refusal-precedence gate, router handler gates (v4 shape on every uuid + all-distinct, byte determinism across invocations, real-engine wire round-trip witness, metadata.version fallback matrix, mutation-key stability).
- Verify: vitest FULL suite 2687/2687 green (110 files); tsc 0 errors in touched files (backlog 1982 = 30 in the parallel session's WIP UI files + 1952 rest ≈ baseline); smoke green; landed as 0f454e4 on dev. UI agent skipped - contested tree (parallel owner session WIP unstaged in ManagementLiabilityTracker/RiskDetailsDialog/CyberIncidentDetail/NIS2SecurityMeasures).
### Cycle 42b - OSCAL import hardening + Federal Hub polish over the parallel GAP-19/GAP-22 landing (2026-08-25)
- test(qa): +20 gates for lib/federal/oscalImport.ts and federalWorkflows.importOscal - real 5MB serialized-length boundary, injected-cap semantics, hostile-Proxy + detection-matrix coverage, 20-input malformed never-throws sweep with validate<->normalize agreement, exported-schema/BAD_REQUEST/exact-shape/determinism/no-DB router contracts (two suites -> 74 tests).
- refactor(ui): FederalHub converted to semantic tokens per UI-STANDARD sec.2 (0 raw palette refs) and the FederalWorkflowsPanels embed corrected to nest inside the layout container; contract layer + panels audited clean.
- chore(backend): oscalImport doc exemplar corrected (nis2/controlHealth.ts); exported oscalImportInputSchema relocated below the module docblock per house layout - behavior unchanged.
- Verify: vitest 2675/2675 green (110 files); tsc backlog unchanged at 1956, 0 errors in touched files; landed as 19e560c on dev. QA follow-ups noted: federal OSCAL exports currently fail their own import validation (non-RFC-4122 uuid, missing metadata.version).
### Cycle 42 - Federal workflow intelligence closed end-to-end: OSCAL import (GAP-19) + live federalWorkflows UI panels (GAP-22) (2026-08-25)
- feat(lib): lib/federal/oscalImport.ts — pure deterministic validator/normalizer for the five OSCAL models (assessment-results / component-definition / plan / system-security-plan / poam): stable kebab-case issue codes, injected 5MB serialized cap, never-throws on hostile input (fuzz-tested), deterministic normalization with last-wins controlId dedupe sorted by controlId asc; zero runtime deps. Conductor fix pre-commit: restored the dropped `merged` declaration in dedupeAndSortControls — a backstop-swallowed ReferenceError that made every valid document normalize to {ok:false}; caught by the new engine suite before landing.
- feat(api): federalWorkflows.importOscal clientProcedure mutation — raw JSON text in, {valid, errors, warnings, normalized} out; unparseable JSON surfaces an invalid-json issue list instead of throwing; no DB reads/writes on the path; exported oscalImportInputSchema/OscalImportInput per sibling-router convention.
- feat(ui): pages/federal/federalWorkflowsApi.ts UI-STANDARD sec.16 typed contract layer over the factory-assembled federalWorkflows router (retry:false hooks, EMPTY_* shapes + predicates, tolerant normalizers, band/status META maps, fixed-clock demo builders, Blob download helpers) + FederalWorkflowsPanels.tsx — SPRS family-deduction score card, DFARS 7012/CIRCIA incident reporting clocks, continuous-monitoring dashboard with POA&M aging table, CMMC readiness band panel, and an export actions row (SSP/POA&M OSCAL JSON downloads, eMASS CSV blob, SAR→POAM sync trigger); Skeleton/EmptyState degradation everywhere, demo mode gated behind a header switch with persistent amber banner, token-only dark-mode safe; additive embed section in FederalHub.tsx behind a Number.isFinite(clientId) guard.
- test(qa): +oscalImport.test.ts (31 engine tests: model detection incl. metadata.oscalModel aliases, every error-code boundary, hostile-input never-throws fuzz set, injected size cap, determinism deep-equal, normalization ordering/dedupe/findings/poam associatedControls fan-out, zero-dep source purity gate); federalWorkflowsRouter.test.ts reconciled to the exact 9-route contract (+importOscal zod edges, UNAUTHORIZED pre-DB, valid-SSP no-DB passthrough, invalid-json issue list, structurally-invalid error surfacing). Conductor completed interrupted parallel-agent work: normalizeReportingClocks import, PanelProps onToggleDemo wiring at all four call sites, hub mount.
- Verify: vitest 2624 → 2655 green across 110 files (+31); tsc backlog 1960 → 1956 lines, 0 new errors in touched files; smoke green; commits 048a07e (salvaged cycle-41 WIP: slaHound NIS2 Art.23 clocks, cyberApi contract layer, GAP-23 schema sync) and 7bb070f pushed to origin/dev.
### Cycle 41 - NIS2 Art. 23 incident clocks on slaHound + cyberApi typed contract layer (2026-08-25)
- feat(agents): slaHound.observe() enforces the NIS2 Article 23 reporting clocks - critical BREACH observations past the 24h Early Warning and 72h intermediate-notification deadlines (stable `nis2-24h-breach:<id>` / `nis2-72h-breach:<id>` keys), warning inside the final <=6h keyed by 6-hour slice (`nis2-24h-warn:<id>:<floor(hoursSinceDetection/6)>`) so dedupe keys are stable within a slice and hand over to BREACH afterwards; significance gate isSignificant || severity critical/high, resolved incidents excluded in SQL, non-Date detectedAt coerced, rejected select degrades to no findings.
- fix(ui)/feat(ui): CyberIncidentDetail, CyberOverview and NIS2CyberResilienceHub token-purity pass per UI-STANDARD sec.2 (85/28/69 -> 14/10/2 raw palette refs) + new pages/cyber/cyberApi.ts sec.16 typed contract layer over the factory-assembled cyber router (getMappings/getIncidents/getIncident/updateIncident, retry:false hooks).
- chore(schema): GAP-23 closed - federal_contracts section_889_status/representative/date synced into schema.ts; bcGuardian rationales cite NIS2 Art. 21(2)(c).
- test(qa): sentinelBots.test.ts pinned to the bucketed Art. 23 dedupe contract (+ shape regex, + inclusive 18h boundary regression) - file suite 50/50. Verify: vitest 2624/2624 green (109 files), tsc 0 errors in touched files (backlog 1956 pre-existing), smoke green; landed as 048a07e on origin/dev + github/dev (co-landed with the parallel owner session mid-cycle).
### Cycle 40 - Notification-center unification landed + GAP-16 closed properly via nordwind IR demo-seed (2026-08-25)
- feat(ui): GlobalNotificationCenter becomes the single notification surface — DB-backed tRPC notifications merged with system notifications, deep-link resolution extracted to pure notificationDestinations.ts (:clientId/:id substitution, external http(s) opens new tab noopener,noreferrer), outside-click close, aria/keyboard a11y, UI-STANDARD §2 token-only surfaces; NotificationCenter.tsx reduced to a re-export shim and DashboardLayout renders one bell.
- conductor salvage of parallel-session WIP: applied the UI-STANDARD §16 typed contract layer over the notifications surface (router-factory inference-collapse TS2339s → 0 tsc errors in touched files); reconciled QA harness fidelity (drizzle records DB column names in tagged ops; raw IS NULL fragment kept via importOriginal; single-eq WHERE not and()-wrapped).
- feat(demo-seed): seedIncidentResponse for --tenant nordwind — 3 realistic closed incidents (incl. two Art. 23-significant with 24h/72h/30d reporting timestamps) + completed tabletop exercise anchored to an auto-provisioned BC/DR plan; idempotent, reset-safe; closes the GAP-16 interim-vs-proper gap.
- fix(api): notifications.markAsRead input z.number().int() (serial-PK boundary caught by the new contract gate).
- test(qa): +32 tests — globalNotificationCenterUiGate (12 static source gates incl. token purity) + notificationsRouterContract (20: exact 12-route map/kind/zod boundaries over mocked db). Stray zzProbe debug test removed. Verify: vitest 2598/2598 green (109 files), tsc 0 errors in touched files (backlog 1960 all pre-existing), smoke green; commit 9ff67ac pushed to origin/dev + github/dev.
### Cycle 39 - Incidents REST surface landed (/api/v1), GAP-18 closed + incident-reporting token purity (2026-08-24)
- feat(api): GET /api/v1/incidents (filters clientId/status/severity enum-validated, limit default 100 cap 200, offset pagination, bounded reads, {data,total}, 400 BAD_REQUEST / 500 INTERNAL_ERROR) + GET /api/v1/incidents/:id (400/404/500 conventions) appended to api-v1.ts under the shared api-key middleware; interrupted partial pass in-file repaired (cap 500->200, offset added). Closes GAP-LOG GAP-18 - incident data was DB-only, blocking the evidence-auto-collector and external bridges.
- ui(cyber): CyberIncidentReporting.tsx token-only conversion per UI-STANDARD sec.2 (54 raw slate/white class refs -> semantic tokens; className-only, logic untouched).
- test(qa): +apiV1IncidentsContract.test.ts (static route/auth/error-code source gate + incident pgEnum schema contract; 29 tests). Verify: partition vitest 49/49 green, tsc 0 new errors in touched files, smoke green; commit 1f510b6 pushed to origin/dev + github/dev. Parallel-session WIP (GAP-17 schema widening, federal-workflows tests, its own incidents harness) detected mid-cycle and preserved uncommitted.
### Cycle 38 - Sentinel Bots / Agent Runtime Phase 1 salvaged & landed, +43 contract/pure tests (2026-08-24)
- feat(agents): interrupted-cycle WIP salvaged and landed as ddf4746 - `sentinel` tRPC router (runNow / listActions inbox / reviewSentinelAction approve-executes-proposal / escalationSweep / sendDigestNow / admin start-stop-status lifecycle) wired into appRouter; agent-runtime heartbeat started from server_entry on listen; Settings > Automation tab UI by the parallel owner session.
- fix(runtime): conductor/backend-agent compile fixes - `SentinelBot` interface gains optional `detectScoreDrop(previousScore, currentScore)` member (bots/types.ts); agentRuntime moduleSummary widened to accept bot error markers `{findings:-1, error}`.
- test(qa): +43 tests (2452 -> 2495, 104 files, all green) - sentinelRouter.test.ts (19): route shape + mutation-vs-query, clientProcedure UNAUTHORIZED asserted pre-DB, admin FORBIDDEN for non-platform-admin roles, zod BAD_REQUEST edges + defaults, no-DB graceful degradation exercising real modules under mocked getDb, DB-independent lifecycle flag flips under fake timers; sentinelBots.test.ts (24): SCHEDULE_MIN_HOURS exact map covering every declared defaultSchedule, detectScoreDrop band boundaries (4 silent | 5 warning | 14 warning | 15 critical, null-input silence, day-scoped dedupeKey, getDb-purity), 7-bot contract (unique ids/moduleKeys cross-checked against the exact modules jsonb runNow enables), observe() returns [] when db unavailable.
- ops(collision): an external git-stash from a parallel session briefly swept the sentinel wiring out of routers.ts/server_entry.ts mid-cycle; recovered via stash apply (their re-edit raced first), wiring ultimately landed inside ddf4746. Collision guard by commit-recency is insufficient against non-committing sessions - flagged in scorecard notes.
### Cycle 37b - Vault section-A hardening follow-through: schema convention parity, +6 coverage/flake-stability tests (2026-08-24)
- refactor(vault): the three cycle-37 input schemas now follow sibling-router convention — exported `credentialVaultRotationScheduleShape` / `credentialVaultExpiryCheckShape` / `credentialVaultAllowlistEvaluateShape` as single source of truth plus `z.infer` types (`CredentialVault*Input`); zod behavior identical.
- test(qa): +6 tests (2371 → 2377) — rotation interval clamp ceiling 36500 (default + override), positive three-octet (/24) wildcard match witness, expiryCheck rejections (missing key / object-shaped credentials / non-int window / >3650 cap / boolean clock), rotationSchedule rejections (missing key vs non-array payload, schema cap 3650 vs engine clamp 36500, negative warnWithinDays), allowlistEvaluate missing-entries rejection + empty-ip-passes-schema→engine denied-invalid semantics, `CREDENTIAL_VAULT_DEFAULT_RATE_LIMIT_PER_MINUTE` asserted against the live route budget on a fresh limiter bucket; flake mitigation: the two multi-scenario its that transiently failed under 100-file load split into smaller units + hostile-loop labels hoisted out of hot loops — no timeout inflation.
- Verify: vitest 2371 → 2377 green (100 files); tsc backlog unchanged at 1938 error lines, 0 errors in touched files.
### Cycle 37 - Credential-lifecycle surface: vault router lifecycle queries wired, wildcard allowlist fix, typed UI panels, +60 tests (2026-08-24)
- feat(vault): credentialVault router lands the three remaining lifecycle procedures from its own contract docstring as protected pure-passthrough queries over lib/security/credentialLifecycle.ts - rotationSchedule (buildRotationSchedule: bands ok/due-soon/due/overdue/never vs injectable clock, policy defaults + overrides), expiryCheck (evaluateCredentialExpiry: valid/expiring/expired/no-expiry/inactive, warning window, coverageRate) and allowlistEvaluate ({normalized, decision} = normalizeAllowlist + evaluateIpAgainstAllowlist, exact > cidr > wildcard precedence, fail-closed); exported zod schemas credentialVaultRotationScheduleInputSchema / credentialVaultExpiryCheckInputSchema / credentialVaultAllowlistEvaluateInputSchema; zod BAD_REQUEST-only, no DB, no state, secrets never echoed.
- fix(security): lifecycleWildcardContains packed fixed prefix octets at the LOW end of the uint32, so leading-octet wildcards (10.*, 10.0.*) matched nothing real; now mask-aligned at the HIGH end (QA-flagged deviation D2, conductor-fixed with regression tests).
- ui(security): credentialVaultApi.ts typed contract layer (EMPTY_* shapes + predicates, retry:false hooks useCredentialVaultRotationSchedule/ExpiryCheck/AllowlistEvaluate, ROTATION_BAND_META / EXPIRY_STATE_META, formatRelativeDays, tolerant normalizers, fixed-clock demo builders, evaluateAllowlistLocally) + CredentialVaultPanels.tsx adds rotation-schedule tiles+table, expiration coverage bar+tiles+table, and an interactive IP-allowlist evaluator card with verdict banner; Skeleton/EmptyState degradation, token-only, dark-mode safe.
- test(qa): +60 tests (46 engine: band boundaries/clamps/ordering/dedupe/capacity/CIDR+wildcard math/never-throws/determinism; 14 router contracts: exact schema exports, UNAUTHORIZED/BAD_REQUEST gates, passthrough shapes, strengthened no-db source scan).
- Verify: vitest 2311 -> 2371 green (100 files); tsc backlog unchanged at 1938 error lines, 0 errors in touched files; smoke green; API-FIRST-INTEGRATION-PLAN.md security checklist rotation/expiration/IP-allowlist items checked off.
### Cycle 36b - Connectors auth hardening: admin-gated connector mutations, factory router, typed ConnectorManager contract layer, +13 tests (2026-08-24)
- feat(connectors): connectors router converted to the house factory `createConnectorsRouter(t, adminProcedure, publicProcedure)`; state-changing mutations (runAll, runProvider, install, uninstall, run) moved from publicProcedure to adminProcedure; reads stay public; exported reusable zod input schemas/types; listTypes/listInstalled reads bounded (.limit(100)); no console.log, no bare Error throws; return shapes unchanged.
- ui(connectors): ConnectorManager.tsx consumes the router through a UI-STANDARD sec.16 typed data-contract layer (connectorsApi cast; factory typing had collapsed client-side inference); auth-aware mutation error toasts ("You don't have permission to manage connectors." on UNAUTHORIZED/FORBIDDEN); token purity fix (text-gray-400 -> text-muted-foreground).
- test(qa): new connectorsAuthGate.test.ts (13 tests) - static source gate (factory signature, exact 10-route/kind map, routers.ts mount tripwire, bounded reads incl. SQL-level LIMIT + single-row aggregates, no bare throws/console.log) + behavioral schema-contract layer over mocked db/collectors.
- Verify: vitest 2298 -> 2311 green (99 -> 100 files); tsc backlog 1945 -> 1938 error lines, 0 new errors in touched files; smoke green.
### Cycle 36 - Evidence-files attach-flow contract landed: hardened+mounted evidenceFiles router, typed Evidence.tsx contract layer, +14 tests (2026-08-24)
- feat(evidence): evidenceFiles router hardened per contract gate - exported zod shape constants/input schemas/z.infer types for all six procedures (single source of truth), TRPCError-only throws (NOT_FOUND/CONFLICT business errors, INTERNAL_SERVER_ERROR wrap via rethrowAsInternal), bounded reads (list .limit(100), existing-links probe .limit(1)); AppRouter mount and EVIDENCE_FILES_LIVE=true now agree end-to-end on Evidence.tsx + AuditHub.tsx.
- ui(evidence): Evidence.tsx consumes the attach flow through a local typed contract layer (evidenceApi = trpc as unknown as EvidenceApi, UI-STANDARD sec.16) with explicit "not available in this deployment" degradation copy on click/drag/change/submit paths.
- test(qa): evidenceFilesContractGate.test.ts (8 static acceptance tests incl. mount-vs-flag tripwires; lands the cycle-35 leftover) + evidenceFilesRouterSchemas.test.ts (6 behavioral schema-parse tests over mocked db).
- Verify: vitest 2298/2298 green (99 files); tsc backlog 1945 -> 1944 lines, 0 new errors in touched files; smoke green.
### Cycle 35 - ICU compact-currency fix, NL contract restored, connector CRUD + roadmap delete landed (2026-08-24)
- fix(currency): compact notation strips ICU trailing zero deterministically ("$120.0K" -> "$120K") via trailingZeroDisplay stripIfInteger + defensive suffix-strip; +3 magnitude regression tests.
- fix(i18n): nl/dashboard.json livePostureScore/commandInterface/governanceHealth restored to the committed test contract; all 66 locale JSONs validated; nav mapping keys verified present in all 6 locales.
- feat(connectors): listTypes/listInstalled/install/uninstall/run/getStats/getRunHistory procedures backing ConnectorManager (malformed history rows guarded); feat(roadmap): admin delete mutation.
- Verify: vitest 2284/2284 green (97 files); tsc 0 errors in touched files, backlog 2113 -> 1945 lines.
### Cycle 34b - server/routers tsc-cleared (-45 backlog), readinessScoring never-throw hardening, +68 QA tests (2026-08-23)
- fix(types): all 11 tRPC router factories under packages/core/src/server/routers/ now typecheck clean (dashboard 17→0, federal 10→0, evidence 4→0, frameworkPlugins/mcp/harmonization/asvs/advisor/llm/compliance/gumroad/knowledgeBase singles) — typed drizzle condition arrays, explicit Map generics over $inferSelect rows, schema-drift casts; zero runtime behavior change. Duplicate getSspControls definition in federal.ts deduped (pre-existing TS1117; runtime-winning copy kept).
- fix(llm): fetchJsonBounded signature accepts the documented timeoutMs-in-init form (runtime already stripped it); fixes llm.ts TS2353.
- fix(readiness): getAuditRiskAlerts never throws on malformed score objects or null/primitive heatmap rows (never-throws convention).
- test(qa): +3 suites / +68 tests — readinessScoring (28, DB-mocked aggregation math incl. coverage-vs-freshness semantics witness), tokens/credits, frameworkHarmonization.
- Verify: vitest 92 files / 2186 tests green; tsc backlog 2113 → 2068 (server/** fully clean; 0 new errors in touched files). Parallel-conductor wave coexisting with f4705f0 (AuditHub/db.ts) via strict file partitions.

### Cycle 34 - AuditHub contract layer + unmounted-evidenceFiles graceful degradation; db.ts type-only cleanup (-74 tsc backlog); contract-gate tripwire (2026-08-23)
- fix(audit): landed interrupted cycle-34 WIP — pages/AuditHub.tsx now uses a local typed data-contract layer (UI-STANDARD sec.16 cast pattern, retry:false, defensive user_metadata read); evidenceFiles.create/.delete verified NOT mounted on the AppRouter (import-only in routers.ts), so file upload/delete degrade to explicit toasts behind EVIDENCE_FILES_LIVE instead of calling missing procedures at mutation time; dead dialog/mutations removed.
- refactor(db): packages/core/src/db.ts type-only cleanup (likeExpr adapter for like(sql``) under drizzle@0.30, typed arrays, two TODO-marked parity casts) — emitted SQL unchanged; tsc backlog 2187 -> 2113 (-74), 0 errors in touched files.
- qa: +packages/core/src/lib/__tests__/auditHubContractGate.test.ts (5 tests incl. mounted-vs-flag tripwire). Verify: vitest 2113 -> 2118 green (89 files); smoke green. Follow-up: Evidence.tsx:86 calls unmounted trpc.evidenceFiles.create.

### Cycle 33 - Hermes orchestration WIP verified & landed; +41 dedicated engine tests; 18 tsc errors cleared (2026-08-23)
- test(agent): first dedicated unit suites for agentDelegationEngine (19 tests: severity gate before any side effect, dispatch/reply shape, honest no-LLM fallback carrying real context, VFS memory record, malformed-input never-throws) and agentRoutineScheduler (22 tests) - previously only mocked indirect coverage existed.
- fix(qa-harness): conductor repaired mock-state leak in agentDelegationEngine.test.ts (missing saveMessageMock.mockClear() in beforeEach caused 14/19 false failures from cross-test call accumulation); suite green after one-line fix.
- verify(ai-agent): scoped tsc - 18 -> 0 errors across toolDispatcher/agentDelegationEngine/agentRoutineScheduler/teammatesRouter; repo backlog 2205 -> 2187 (-18, nothing new); full vitest 88 files / 2113 tests green (+4 files / +106 vs baseline incl. cycle-32 vault tests); smoke green.
- process: second live parallel-conductor detected mid-cycle (credential-vault crew writing 09:35-09:54); file-partitioned agents kept zero overlap; parallel conductor swept this cycle's finished work into b301d7a at 10:08 (all 8 partition files verified present) - no duplicate commit needed.

### Cycle 32 - Credential Vault backend (AES-256-GCM cosv1 vault, audit hash-chain, rate limiter, 2FA policy) + guardrails power-multiplier increment landed & repaired (2026-08-23)
- feat(security): credentialVault tRPC router + lib/security/credentialCrypto.ts per the cycle-31 UI contract — closes the API-FIRST plan's P0 credential-security block (encryption at rest, decrypt-on-request, audit logging, rotation/expiration/2FA posture checklist).
- feat(ai-agent): cycle-31 WIP landed — toolDispatcher real-data FAIR Monte Carlo contract, delegation/routine-scheduler expansion; teammatesRouter repaired to the new engine signatures (-18 tsc errors in touched files).
- Verify: vitest 2007 -> 2113 green (88 files), tsc backlog 2205 -> 2187 (0 new), smoke green; commits b301d7a + c23e7a7 pushed to origin/dev.

### Cycle 30 - Local LLM air-gapped mode (auto-discovery, sovereign execution modes, model catalog); registerSummary injectable-clock fix; AddonScheduler guard (2026-08-22)
- feat(llm): local LLM support landed — lib/llm/localLLMDiscovery.ts (Ollama/LM Studio/vLLM/LocalAI probes with hard timeouts, >=10-model recommended catalog across 3 hardware tiers, <think>-block output cleaner), execution modes hybrid/local_only/cloud_only persisted on LLMService, llm router procedures detectLocalRuntimes/getRecommendedLocalModels/getExecutionMode/setExecutionMode/listAvailableModels, admin LLMSettings UI.
- fix(addons): AddonScheduler guards incompatible DB clients — warn-once + safe no-op tick instead of per-tick "db.select is not a function" error spam.
- fix(vulnerabilityMgmt): registerSummary input schema accepts optional `now` and forwards it to the engine's injectable clock; time-bombed router test fixture pinned to NOW repaired (overdueCount drift).
- Verify: targeted suites green (localLLMSupport + vulnerabilityMgmtRouter 30/30), smoke green; two parallel-conductor collisions handled (b1942c0 mid-cycle tree sweep; foreign memory/knowledge WIP active at close, left untouched).

### Cycle 29 - routers.ts collision-damage repair: deduplicated import block, -441 tsc backlog errors (2026-08-22)
- fix(routers): packages/core/src/routers.ts shipped in 6dc6923 with two overlapping import generations (parallel-cycle collision) - 132 duplicate import statements + duplicated "// Router index" banner causing 436 TS2300 duplicate-identifier errors and ~582 total cascade errors. Repaired with a statement-aware dedupe (scratch/cycle29-dedupe-routers.cjs) that parses multi-line imports atomically, keeps the first occurrence of each of the 167 unique statements, removes exactly 172 lines, preserves CRLF and brace balance. tsc -p packages/core/tsconfig.json --noEmit: 2615 -> 2174 error lines (-441); routers.ts itself 491 -> 55 (chronic pre-collision backlog unchanged).
- Verify: npx vitest run 80 files / 1944 tests all green on HEAD+repair; npm run smoke green ("Phase 2 ready"). No runtime code changed beyond import de-duplication (git diff = pure deletions).

### Cycle 28 - Multi-agent guardrails & provenance hardening landed; interrupted cycle-28 WIP repaired and stabilized (2026-08-22)
- Backend: 7 new lib/agent guardrail engines (promptInjectionGuard, dlpSanitizer, actionGatekeeper, toolDispatcher, rateLimiterCircuitBreaker, policyVectorRag, provenanceLedger with SHA-256 hash-chained ledger + audit certificates); teammatesRouter extended (guardrails status, approvals-in-loop tool execution, audit certificate, sandbox control, provenance ledger) and mounted behind protectedProcedure; ApprovalItem.type union gained terraform_apply.
- UI: new pages/agent/agentCockpitApi.ts typed contract layer (UI-STANDARD sec.16) rewiring MultiAgentChatCockpit + ApprovalInboxView off raw trpc calls; new CredentialVaultPanels read-only section on Security Settings with EmptyState degradation until credentialVault.* lands; AgentReportsPage exported from AgentPage, repairing the /agent/reports lazy route broken since the Hermes cockpit commit.
- QA/housekeeping: +guardrails/power-multiplier + teammatesRouter contract tests; internalLinks OneDrive budget 30s -> 180s; uiTokenPurity gate extended to the four agent surfaces; SecuritySettings MFA listFactors cast + invalid breadcrumb prop removed.
- Verify: vitest 1944/1944 green (80 files); tsc touched-files clean, total backlog 2626 -> 2610; smoke exit 0; committed to dev (6d68558), pushed origin/dev + github/dev.
### Cycle 27 - QA hardening: uiTokenPurity gate extended to risk-quantification files; independent re-verification of parallel cycle 26 (2026-08-21)
- QA: packages/core/src/lib/__tests__/uiTokenPurity.test.ts PAGES list now strictly covers pages/riskQuantificationApi.ts + pages/cyber/RiskQuantificationPanels.tsx (zero forbidden surface tokens found). Suite 1891 -> 1893 (78 files), all green; tsc backlog unchanged (2033 total, 0 errors in any riskQuantification.* file); smoke green.
- Conductor note: cycle 26 landed from a parallel conductor mid-session (96285ae/f77fe5c/da63d04, already pushed). This cycle independently re-verified it (vitest + tsc + smoke) and ran BACKEND/UI/QA review passes over the new engine/router/panels - no defects found, no source changes required.
### Cycle 26 - NIS2 Risk Quantification Engine: quantitative appetite calculator, 5x5 LxI matrix, residual tracking & treatment plans (NIS2 plan Phase 1 Task 1.2) (2026-08-21)
- Backend lib/nis2/riskQuantification.ts: new pure deterministic NIS2 risk quantification engine (zero deps, never throws, injectable clock) - computeRiskAppetite (score = likelihood x impact 1-25 from numbers or scale keywords rare..almost-certain / negligible..severe, out-of-range numbers and unknown words INVALID not clamped; verdicts within/tolerance/exceeded with STRICT > comparisons vs appetiteScore default 12 and tolerancePct default 10 (exceeded threshold = appetite x (1+tol/100)); optional EUR annual-loss appetite check driving an extra recommendation; avgScore 1dp; appetiteUtilizationPct round1 clamped 0-100; breaches sorted score desc then id asc capped 5; recommendations capped 5 in fixed order omitting zero buckets, standing review recommendation only when assessedCount > 0; totalRisks counts object rows incl. garbage per house style; malformed/non-array -> EMPTY_RISK_APPETITE), buildQuantitativeMatrix (fixed 25-cell grid always present ordered likelihood 5..1 then impact 1..5 with per-cell counts; bands low 1-4 / medium 5-9 / high 10-15 / critical 16-25; byBand rollup; avgScore 1dp; topRisks capped 5 sorted score desc then id asc numeric-before-string; malformed -> EMPTY_RISK_MATRIX with empty grid), trackResidualRisk (inherent = inherentScore override else LxI; residual = residualScore override else round1(inherent x (1-effectiveness)); effectiveness from controlEffectiveness/100 else controls-count map 0->0/1->20%/2->35%/3->50%/>=4->60%; delta = max(0, inherent-residual); reductionPct 1dp; direction reduced/increased/unchanged; rows sorted residual desc then id asc; aboveAppetiteCount STRICT > appetiteScore default 12; avgReductionPct 1dp over inherent>0 rows; worst capped 5; malformed -> EMPTY_RESIDUAL_TRACKING), generateTreatmentPlans (plans only status open|mitigating|undefined risks with score >= threshold default 10; strategy ladder >=16 mitigate / 10-15 transfer when any control matches /transfer|insurance|contract/i else mitigate; priorities P0>=16/P1>=10/P2>=5/P3 else counted for ALL assessable risks; P0 plans prepend escalation action capped 5; P0/P1 deadlines = injected clock + horizonDays default 30 ISO, others null; owner passthrough or 'unassigned'; plans sorted priority asc then score desc then id asc; byStrategy planned-only; nextDeadline earliest non-null or null; malformed -> EMPTY_TREATMENT_PLAN_RESULT).
- Backend server/routers/riskQuantification.ts: createRiskQuantificationRouter(t, protectedProcedure) - protected pure appetite / matrix / residual / treatmentPlans queries with exported zod schemas (riskQuantificationAppetiteInputSchema with nested config block, MatrixInputSchema, ResidualInputSchema, TreatmentPlansInputSchema + shared clockOptionSchema now string|number / clock fn, RiskRowSchema loose scale fields engine-coerced internally); zod BAD_REQUEST only, no DB access; wired as `riskQuantification:` in routers.ts.
- UI: pages/riskQuantificationApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_RISK_APPETITE/_RISK_MATRIX/_RESIDUAL/_TREATMENT_PLANS, retry:false hooks useRiskAppetite/useRiskMatrix/useResidualRisk/useTreatmentPlans, QUANT_BAND/APPETITE_VERDICT/RESIDUAL_DIRECTION/TREATMENT_PRIORITY/TREATMENT_STRATEGY meta helpers + quantBandForScore/residualBarClass, DEMO_QUANT_RISKS + buildDemo* per sec.17 with fixed demo clock); pages/cyber/RiskQuantificationPanels.tsx "NIS2 Risk Quantification" section (appetite strip with utilization bar + within/tolerance/exceeded tiles + breach list + recommendations, 5x5 heat matrix grid with token-based cell fills + band counters + top-risks list, residual table with inherent/residual/reduction bars + direction badges + above-appetite highlight, treatment plan cards with priority/strategy badges + action checklists + deadline countdowns; demo-mode amber banner behind an off-by-default Switch, Skeleton/EmptyState degradation; token-only, dark-mode safe); additive embeds on RiskRegister overview tab (+fragment wrapper) and RiskHeatmapPage.
- QA: +54 tests (31 engine incl. verdict boundaries strict > at appetite and appetite*(1+tol), invalid-scale exclusion not clamping, utilization clamp, breach sort/cap, loss-appetite recommendation, matrix band edges 4/5 9/10 15/16 + fixed-grid ordering + top-risk tie-breaks, residual derivation maps + overrides + increased-direction edge + aboveAppetite strictness, treatment threshold/strategy-keyword/status-eligibility/P0 escalation cap/deadline clock injection ISO-epoch-Date-factory-throwing/sort/owner fallback, cross-cutting determinism/never-throws/EMPTY-freeze/garbage-row counting; 11 router contract incl. route shape/no-mutations, protected UNAUTHORIZED, zod BAD_REQUEST type mismatches, loose-boundary coercion witness, clock option string/number/fn accepted + Date rejected, exported schemas, no-DB + no-Math.random purity source scan). Suite 1837 -> 1891 green (76 -> 78 files); tsc 2033 baseline unchanged (0 errors in touched files); smoke green; committed to dev (96285ae). All three async agents (BACKEND/UI/QA) died to upstream timeouts with partial work - conductor rebuilt the backend, both test files and the full UI layer from the shared spec, reconciled the UI agent's coherent RiskHeatmapPage embed, and fixed 3 self-inflicted defects mid-cycle (standing recommendation emitted on zero assessed data vs EMPTY contract; breach-count arithmetic slip 15 > 13.2; JSX fragment wrapper for the two-sibling embed). Suite 1837 -> 1891 green (78 files); tsc 2033 baseline unchanged (0 errors in touched files); smoke green; committed to dev (96285ae), pushed to origin/dev + github/dev.
### Cycle 25 - NIS2 Threat Landscape Integration: ENISA taxonomy classification, sector scenarios, TARA template & exposure summary (NIS2 plan Phase 1 Task 1.1) (2026-08-21)
- Backend lib/nis2/threatLandscape.ts: new pure deterministic NIS2 threat landscape engine (zero deps, never throws, injectable clock) - classifyThreatEvent (ENISA taxonomy keyword classification over title+description with title-wins precedence; matchedKeywords deduped case-insensitively preserving taxonomy map order TH-001..TH-008; fallback TH-000/Other impactLevel Medium confidence 0; confidence = min(1, 0.2 x distinct keyword hits) rounded 1dp; nis2Articles from the base threat or []), generateThreatScenarios (built-in sector catalog SC-FIN-01 / SC-HC-01 / SC-MFG-01 + general Any scenarios; sector matching exact-token or substring either direction, case-insensitive, non-string treated as absent; sector scenarios PLUS Any scenarios on match, Any-only fallback otherwise; positive limit floored and applied last, non-positive/NaN/Infinite ignored; deterministic catalog order), buildTaraTemplate (asset x scenario cross-product rows; inherentLikelihood High 3/Medium 2/Low 1, inherentImpact Critical 4/High 3/Medium 2/Low 1, unknown -> 1; riskScore = likelihood x impact banded critical >=9 / high >=6 / medium >=3 / low; rows sorted riskScore desc then assetName asc then scenarioId asc; perBand counts + avgRiskScore 1dp + topRisks capped 5; mitigations from recommendedControls falling back to mitigations else []), summarizeThreatLandscape (totalEvents incl. garbage rows; category rollups sorted categoryId asc; severity counts always 5 buckets unknown-coerced; recentEvents top 5 occurredAt desc then id asc numeric-before-string ties, invalid/missing occurredAt last by id asc emitted occurredAt null; 30-day trend windows vs injected clock excluding future-dated+invalid; exposureScore = round1(100 x sum(weights)/(n x 5)) weights critical 5/high 4/medium 3/low 2/unknown 1 clamped [0,100]; up to 5 fixed deterministic recommendations in severity precedence omitting zero buckets; sector passthrough never changes output; malformed input -> zeroed EMPTY_THREAT_LANDSCAPE_SUMMARY).
- Backend server/routers/threatLandscape.ts: createThreatLandscapeRouter(t, protectedProcedure) - protected pure classify / scenarios / tara / summary queries with exported zod schemas (six input/item schemas with STRICT enums rejecting loose likelihood/criticality strings at the boundary while the engine coerces internally); zod BAD_REQUEST only, no DB access; wired as `threatLandscape:` in routers.ts.
- UI: pages/threatLandscapeApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_THREAT_CLASSIFICATION/_SCENARIOS/_TARA_TEMPLATE/_THREAT_SUMMARY, retry:false hooks, SEVERITY/RISK_BAND/TREND_DELTA meta helpers, DEMO_* + buildDemo* per sec.17); pages/cyber/ThreatLandscapePanels.tsx "NIS2 Threat Landscape" section on CyberDashboard (classification card, sector scenario cards, TARA table, summary strip with exposure score; demo-mode amber banner behind an off-by-default Switch, Skeleton/EmptyState degradation; token-only, dark-mode safe - independent UI audit passed with zero edits required); CyberDashboard.tsx additive embed (+4 lines).
- QA: +43 net tests (113 total across two new files: 92 engine incl. determinism/never-throws/EMPTY-freeze/garbage-row coverage + 21 router contract incl. protected UNAUTHORIZED, strict-enum BAD_REQUEST rejections, exported schemas, no-DB scan). Suite 1794p/38f -> 1837/1837 green (76 files); tsc 2033 baseline unchanged (0 errors in touched files); smoke green; committed to dev (4660391), pushed to origin/dev + github/dev. Conductor completed the interrupted parallel WIP from 3 async agents and fixed 2 test-fixture defects (TARA sorting fixture gave assets a and c identical "High" criticality while expecting divergent scores; recentEvents fixture relied on the ev() helper defaulting occurredAt so the missing-field path was never exercised).
### Cycle 24 - NIS2 Evidence Repository: ENISA-keyed evidence suggestions, audit trail & quality analysis (NIS2 plan Phase 6 Task 6.2) (2026-08-21)
- Backend lib/nis2/evidenceRepository.ts: new pure deterministic NIS2 Evidence Repository engine (zero deps, never throws, injectable clock) - suggestEvidence (13-measure ENISA catalog 1.1..12.1 -> NIS2 Art. 21(2) categories a-j with evidenceTypes/freshnessDays/baseScore; measureId exact-match + category substring + requirementText keyword scoring; limit applied last; sorted baseScore desc then measureId asc; deterministic; malformed -> EMPTY_EVIDENCE_SUGGESTIONS), buildEvidenceAuditTrail (events sorted most-recent-first with null updatedAt last + id asc tie-break; evidenceId falls back to String(id); status coerced to the 6-value enum pending/collected/verified/rejected/expired/not_applicable; daysSinceUpdate floor whole days; summary totals/byStatus/withOwner/withFiles; malformed -> EMPTY_EVIDENCE_AUDIT_TRAIL), analyzeEvidenceQuality (freshness expired < now / expiring <= 90d / stale never-verified-or-past-interval / fresh; cadence n-a / overdue strict > interval / due-soon >= 75% interval whole-day floors / on-track; composite qualityScore = status base 50/35/20/15/0 + freshness bonus 30/15/0 + cadence bonus 30/15/5/0 + 5 when files attached, clamped 0-100; band strong >= 80 / adequate >= 55 / weak; daysUntilExpiry; owner passthrough (trimmed, null when empty); nextAction ladder; overall avgQualityScore 1dp + coverageRate = verified/total 1dp + 8-key counts + recommendations capped 5 in fixed order; malformed -> EMPTY_EVIDENCE_ANALYSIS).
- Backend server/routers/evidenceRepository.ts: createEvidenceRepositoryRouter(t, protectedProcedure) - protected pure suggest / auditTrail / analyze queries with exported zod schemas (evidenceRepositorySuggestInputSchema / AuditTrailInputSchema / AnalyzeInputSchema + row/item schemas, shared clockOptionSchema now string|number / clock fn); zod BAD_REQUEST only, no DB access; wired as `evidenceRepository:` in routers.ts.
- UI: pages/evidenceRepositoryApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_EVIDENCE_SUGGESTIONS/AUDIT_TRAIL/ANALYSIS, retry:false hooks, STATUS/FRESHNESS/CADENCE/BAND meta helpers, DEMO_EVIDENCE_ROWS + buildDemo* per sec.17 with fixed demo clock); pages/EvidenceRepositoryPanels.tsx "NIS2 Evidence Repository" section on the Evidence page (evidence health strip with avg quality score + coverage rate + recommendation chips; evidence quality table with ScoreBar, freshness/cadence badges, owner column, days-until-expiry countdown and next-action guidance; audit-trail feed with status chips, owner, file counts and relative update time; suggestion cards keyed to ENISA measures with Art 21(2) category + evidence types + freshness window; demo-mode amber banner, Skeleton 1:1 + EmptyState "Connect the evidenceRepository.<proc> API" degradation; token-only, dark-mode safe); Evidence.tsx additive embed.
- QA: +98 tests (84 engine incl. catalog integrity 13 measures ascending/frozen/no dupes/article coverage a-j, filter/limit semantics, score math + clamp + determinism, audit-trail coercion/sort/tie-break/summary, quality boundaries expired/expiring 90/91d, stale strict > interval, due-soon 75% floor, band edges 80/75/55/50, next-action ladder, counts/avg/coverage math, recommendations order/cap/omit-zero, clock injection ISO/epoch/Date/factory/throwing-factory + never-throws; 14 router contract incl. route shape/no-mutations, protected UNAUTHORIZED, zod BAD_REQUEST type mismatches, exported schemas, no-DB + no-Math.random purity source scan; 2 uiTokenPurity gate entries). Suite 1626 -> 1724 (72 -> 74 files), all green; tsc 2033 (0 new in touched files - pre-existing dashboard/evidence/federal/routers backlog unchanged); smoke green; committed to dev (3d49dbf), pushed to origin/dev + github/dev. Conductor completed the interrupted cycle-24 WIP: fixed 3 test defects (purity source scan strips comment blocks so rule descriptions don't trip the check; counts.verified = rows with status "verified" = 3 incl. expired-but-verified row, consistent with the recommendations test; determinism test defines its own fixtures) + added owner passthrough to analysis rows (engine interface + mapping, contract layer type, demo builder) resolving the 2 new tsc errors; removed stray `$null` artifact from the tree.

### Cycle 23 - NIS2 Policy Center: Art 21(2) policy template catalog, ISO gap analysis, approval workflow & version history (NIS2 plan Phase 6 Task 6.1) (2026-08-21)
- Backend lib/nis2/policyTemplates.ts: new pure deterministic NIS2 Policy Center engine (zero deps, never throws, injectable clock) - getNis2PolicyTemplates (built-in catalog of 13 templates, one per ENISA measure row of the plan mapping table 1.1..12.1 -> NIS2 Art. 21(2) category a-j -> ISO/IEC 27001:2022 controls; exact category/measureId filters, case-insensitive search over title/summary/enisaMeasureTitle, positive-integer limit applied last; items sorted enisaMeasureId NUMERICALLY asc (1.1, 2.1, .., 9.1, 10.1, 11.1, 12.1) then title asc; malformed -> EMPTY_POLICY_TEMPLATE_LIST), analyzePolicyGap (template covered when ANY implemented policy shares an ISO control or a >=3-char title word case-insensitively; every row counts as implemented regardless of status; coverageRate = covered/total 1dp 0 on zero denominator; gaps carry only uncovered controls + deterministic "Adopt ..." recommendation; gaps sorted numeric enisaMeasureId asc then title asc; byIsoControl rollups with templateCount + covered/gap status sorted isoControl asc; recommendations capped 5; malformed -> EMPTY_POLICY_GAP_ANALYSIS), runPolicyApproval (status normalized draft/in_review/approved/rejected/changes_requested/unknown; verdict precedence Approved > Rejected > Changes requested from reviewer decisions, else status-derived Draft / Awaiting review / Approved / Rejected / Changes requested / Unknown, lowercase "unknown" only when no status was supplied; approvalCount/rejectionCount/changesRequestedCount/pendingCount; reviewProgress = decisions/requiredApprovals*100 1dp with default requiredApprovals 1 (absent counts as valid, explicit invalid zeroes progress); complete = approvalCount >= requiredApprovals; overdue strict at submittedAt + slaDays*86400000 (default 14d); daysInReview floor; 4-step workflow with done/current/pending; reviewers enriched with decision status, sorted decidedAt asc nulls last then id asc; malformed -> EMPTY_POLICY_APPROVAL), trackPolicyVersions (versions sorted createdAt asc nulls last then label asc; last sorted entry 'current' unconditionally, others 'superseded' unless draft-normalized; latestVersion/currentVersion = last entry with a real label (garbage rows never become current); createdAt emitted ISO or null; draft/approved/superseded counts; changes = non-empty changeSummaries in order; EMPTY_POLICY_VERSION_HISTORY zeroed + frozen with policyId undefined).
- Backend server/routers/policyTemplatesNis2.ts: createPolicyTemplatesNis2Router(t, protectedProcedure) - protected pure templates / gapAnalysis / approvalWorkflow / versionHistory queries with exported zod schemas (policyTemplatesNis2TemplatesInputSchema / GapAnalysisInputSchema / ApprovalInputSchema / VersionsInputSchema + item schemas, shared clockOptionSchema now string|number / clock fn); zod BAD_REQUEST only, no DB access; approvalWorkflow adds the contract-named `currentStatus` alongside the engine's `status`; wired as `policyTemplatesNis2:` in routers.ts.
- UI: pages/policyTemplatesNis2Api.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false hooks, NIS2_* types mirroring the backend contract incl. currentStatus + verdict strings, GAP/APPROVAL/VERSION meta helpers, DEMO_* + buildDemo* per sec.17); pages/cyber/PolicyCenterPanels.tsx "NIS2 Policy Center" section on CyberDocumentation (template catalog table with ENISA measure + Art 21 category + ISO controls + review cadence; gap analysis with coverage rate + uncovered templates + per-ISO-control rollups + top recommendations; approval workflow tracker with currentStatus badge, verdict, reviewProgress bar, 4-step pipeline + reviewer decision table; version history with latest/current + superseded/draft statuses + change log; demo-mode amber banner; Skeleton 1:1 + EmptyState "Connect the policyTemplatesNis2.<proc> API" degradation; token-only, dark-mode safe); CyberDocumentation.tsx additive embed.
- QA: +127 tests (99 engine incl. numeric ENISA ordering 1.1..12.1, filters/search/limit semantics, ISO-control + title-word coverage matching, gap sort/tie-break/recommendation cap, verdict precedence + status-derived verdict casing, count tallies, reviewProgress 1dp rounding + default requiredApprovals + invalid-zero + complete boundary, overdue strict boundary + sla override + never-overdue invalid dates, daysInReview floor, step statuses per status, reviewer enrichment/sort/garbage rows, version sort/tie-break/latest-current/draft handling/counts/change summaries/label fallback/garbage rows, EMPTY shape freezing + never-throws + determinism + clock injection ISO/epoch/Date/factory/throwing-factory; 26 router contract incl. route shape/no-mutations, protected UNAUTHORIZED, zod BAD_REQUEST type mismatches (Date objects rejected, loose enum strings engine-coerced), exported schemas, no-DB source scan, currentStatus witness; 2 uiTokenPurity gate entries for the new UI files). Suite 1499 -> 1626 (72 files), all green; tsc 2033 (0 new in touched files - pre-existing dashboard/evidence/federal/routers backlog unchanged); smoke green; committed to dev (501e14e), pushed to origin/dev + github/dev. Conductor reconciled the interrupted cycle-23 WIP (6 new files + 3 modified from the parallel build) to the test contract: numeric ENISA sort comparator in getNis2PolicyTemplates + analyzePolicyGap, title-cased verdicts (Draft/Approved/Rejected/Changes requested/Awaiting review/Unknown; lowercase "unknown" only for missing status), added rejectionCount/changesRequestedCount/pendingCount, reviewProgress/complete against default requiredApprovals=1 with explicit-invalid zeroing, latestVersion skips empty garbage labels, EMPTY_POLICY_VERSION_HISTORY.policyId undefined, router approvalWorkflow exposes currentStatus; internalLinks timeout in the parallel run confirmed as the known OneDrive flake (passes standalone in <1.5s).

### Cycle 22 - NIS2 continuous compliance monitoring: posture engine, evidence coverage & audit-ready report generation (NIS2 plan Phase 5 Task 5.2) (2026-08-20)
- Backend lib/nis2/complianceMonitor.ts: new pure deterministic NIS2 continuous compliance monitoring engine (zero deps, never throws, injectable clock) - computeCompliancePosture (equal-weight average of valid finite scores, clamped 0-100, 1 decimal; bands on the UNROUNDED average Strong >= 85 / Developing >= 65 / At Risk >= 40 / Critical < 40, empty -> "No Data" score 0; coverageRate = valid/total 1 decimal, 0 when total 0; driftPts = mean of per-measure (score - baselineScore) 1 decimal, null when no baseline; trend improved >= +0.05 / declined <= -0.05 / stable, n-a when drift null; statusCounts strong/developing/atRisk/critical/noData; topGaps numeric-score measures only, score asc then measureId asc, sliced 5; totalMeasures/assessedMeasures + deterministic verdict; malformed -> EMPTY_COMPLIANCE_POSTURE), trackEvidenceCoverage (covered = finite evidenceCount > 0; expiry only for covered items with valid expiresAt: expired < now, expiring 0 <= delta <= 90 days (exactly 0 and exactly 90d expiring), current otherwise, invalid -> n-a; coverageRate 1 decimal; avgEvidencePerControl 1 decimal; byMeasure rollup coverageRate asc then measureId asc; items controlId asc null last; malformed -> EMPTY_EVIDENCE_COVERAGE), generateAuditReport (postureScore derived from measures when absent else explicit; coverageRate from evidenceSummary total/covered or explicit; topGaps = assessed measures below the Strong bar (< 85), capped 5; section statuses posture pass>=85/warn>=65/fail, evidence pass>=80/warn>=50/fail, gaps fail when any gap else pass, measures informational; deterministic markdown report with entity/sector/generatedAt from injected clock/measure rows/top gaps/recommendations; recommendationCount = topGaps.length; malformed -> EMPTY_AUDIT_REPORT report "").
- Backend server/routers/complianceMonitor.ts: rewritten from the old DB-backed router to a pure facade - createComplianceMonitorRouter(t, protectedProcedure) with protected pure posture / evidenceCoverage / auditReport queries, exported zod schemas (complianceMonitorPostureInputSchema / complianceMonitorEvidenceCoverageInputSchema / complianceMonitorAuditReportInputSchema + item schemas, shared clockOptionSchema now string|number / clock fn), zod BAD_REQUEST only, no DB access; wired as `complianceMonitor:` in routers.ts (protectedProcedure, old premiumClient/admin wiring removed).
- UI: pages/complianceMonitorApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false hooks useCompliancePosture/useEvidenceCoverage/useAuditReport, POSTURE_STATUS_META / TREND_META / EVIDENCE_STATUS_META / EXPIRY_META / AUDIT_SECTION_STATUS_META + formatScore / coverageBarClass helpers, DEMO_* + buildDemo* per sec.17 with fixed demo clock); pages/cyber/ComplianceMonitorPanels.tsx "Continuous Compliance Monitoring" section on CyberDashboard (posture scorecard with gauge + status counts + top gaps; evidence coverage with per-measure rollups + expiry badges; audit report preview with section status chips + markdown body; demo-mode amber banner; Skeleton 1:1 + EmptyState degradation; token-only, dark-mode safe); CyberDashboard.tsx additive embed. Component migrations off the removed DB-backed procedures: ComplianceHealthWidget + ContinuousComplianceScore now use the pure complianceMonitor.posture via the contract layer (removes the Math.random()-based score); MSSPRevenueWidget revenue now computed from trpc.clients.list via the established as-any cast (fix 3c2ca89 - type-clean, 0 new tsc errors).
- QA: +94 tests (74 engine incl. posture band boundaries 85/84.9 65/64.9 40/39.9 on unrounded averages, drift thresholds, topGaps sort/tie-break/slice-5, coverage math, expiry boundaries 1ms-before/at-0/90d/90d+1ms, byMeasure rollup/sort, audit report sections/derivation/determinism/clock injection/garbage rows, EMPTY shape freezing, never-throws; 18 router contract incl. protected UNAUTHORIZED, zod BAD_REQUEST, exported schemas, no-DB source scan; 2 uiTokenPurity gate entries). Suite 1405 -> 1499 (70 files), all green; tsc 2033 (-8 vs 2041 baseline; 0 new in touched files after MSSPRevenueWidget as-any cast fix 3c2ca89 - pre-existing backlog unchanged); smoke green. Conductor reconciled 8 test failures (round1 float-error epsilon + -0 normalization, audit topGaps below-Strong semantics + recommendationCount, parseable clockOptionSchema) and migrated 3 legacy widgets off the removed DB-backed complianceMonitor procedures (ComplianceHealthWidget/ContinuousComplianceScore -> pure posture; MSSPRevenueWidget -> trpc.clients.list via as-any cast).

### Cycle 21 - NIS2 advanced security testing: pen test scheduler, red team framework, benchmark compliance & scan coverage (NIS2 plan Phase 5 Task 5.1) (2026-08-20)
- Backend lib/nis2/securityTesting.ts: pure deterministic NIS2 advanced security testing engine (zero deps, never throws, injectable clock) - planPenetrationTest (test-type/status lifecycle draft->scheduled->in_progress->completed|reported; overdue = scheduled OR in_progress AND scheduledDate + windowHours < now, strict, default window 336h with per-test override; risk-tier frequency critical 91d quarterly / high 182d semi-annual / medium 365d annual / low+info 730d biennial, unknown -> annual; nextDueDate = lastCompletedAt + cadence, fallback scheduledDate; countsByStatus/countsByType with kebab->camel keys; tests sorted status asc/type asc/id asc; overdueTests scheduledDate asc then id asc; malformed -> EMPTY_PEN_TEST_PLAN), runRedTeamExercise (7 MITRE ATT&CK phases recon/weaponization/delivery/exploitation/lateral-movement/exfiltration/reporting with tacticCodes; phase status pending|active|completed, invalid -> pending; isOverdue active AND due < now where due = exercise endAt else phase estimatedEndAt; overall status completed (all done) > paused (explicit) > active (any active) > not-started; verdict read only when completed (no-breach|contained|breached); participant roles operator|observer|decision-maker, invalid -> observer, dedup by id last-wins, role order then name then id; summary total/completed/active/pending/overduePhases), assessSecurityBenchmarks (CIS Controls v8 IG1-IG3 + NIST CSF 2.0 govern..recover, benchmark aliases nist-csf/nistcsf/csf; status pass|fail|not-applicable|na|n-a case-insensitive, unknown rows skipped; categoryScore = pass/(pass+fail)*100 1 decimal, 0 when no applicable; overallScore = passRate = total pass / applicable 1 decimal; topGaps failing controls sorted category order then controlId asc capped 5 with stable keyword remediation catalog), trackScanCoverage (asset classes external-ip/internal-host/web-app/api/cloud-asset, unknown skipped; due = dueAt override else lastScanAt + scanFrequencyHours, rows without timing skipped; inSla = due >= now; byClass rollups array in catalog order {assetClass, assetClassName, total, covered, overdue, coverageRate}; assets sorted class order/name/id with daysOverdue floor signed). Complements lib/nis2/securityMetrics.ts (cycle 20) - same house pattern.
- Backend server/routers/securityTestingNis2.ts: createSecurityTestingNis2Router(t, protectedProcedure) - protected pure planTest / redTeam / benchmarks / scanCoverage queries with exported zod schemas (securityTestingNis2PlanInputSchema / securityTestingNis2RedTeamInputSchema / securityTestingNis2BenchmarksInputSchema / securityTestingNis2ScanCoverageInputSchema + per-item schemas, shared clock option now string|number / clock fn, loose nullish fields with engine coercion); zod BAD_REQUEST only, no DB access; wired as `securityTestingNis2:` in routers.ts (2 lines).
- UI: pages/securityTestingNis2Api.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_PEN_TEST_PLAN / EMPTY_RED_TEAM_EXERCISE / EMPTY_BENCHMARK_ASSESSMENT / EMPTY_SCAN_COVERAGE, retry:false hooks usePenTestPlan/useRedTeamExercise/useBenchmarkAssessment/useScanCoverage, TEST_TYPE_META / PEN_STATUS_META / FREQUENCY_META / PHASE_STATUS_META / RED_TEAM_PHASE_META / EXERCISE_STATUS_META / VERDICT_META / BENCHMARK_CATEGORY_META / ASSET_CLASS_META + formatNextDueDate / coverageBarClass helpers, DEMO_PEN_TESTS / DEMO_RED_TEAM_EXERCISE / DEMO_BENCHMARK_ASSESSMENTS / DEMO_SCAN_ASSETS + buildDemo* per sec.17 with fixed demo clock); pages/cyber/SecurityTestingPanels.tsx "NIS2 Advanced Security Testing" section on the SecurityTesting page (penetration test scheduler with per-status/type counts + frequency + next-due + overdue badges; red team kill-chain tracker with 7 MITRE ATT&CK phase chips + participant role counts + verdict; benchmark compliance CIS + NIST category scorecards + top remediation gaps; scan coverage per-class SLA rollups + overdue scan list; demo-mode amber banner; Skeleton 1:1 + EmptyState "Connect the securityTestingNis2.<proc> API" degradation; token-only, dark-mode safe); SecurityTesting.tsx token-only polish (slate/rose/indigo -> tokens, dark-mode variants) + additive embed.
- QA: +95 tests (73 engine incl. overdue strict boundaries 335/336/337h + per-test window override + no-date never overdue + status gating, bucket key order, frequency math + nextDueDate, clock injection ISO/epoch/Date/factory/throwing-factory, red-team phase matching by catalog id + status precedence + overdue boundary + participant role fallback/dedup/sort + summary counts, benchmark family/category aliases + status coercion + score math + topGap sort/cap + remediation keyword match, scan SLA boundary + dueAt precedence + class rollups + sort + daysOverdue math, malformed/determinism/never-throws; 21 router contract incl. route shape/no-mutations, protected UNAUTHORIZED, zod BAD_REQUEST on type mismatches, exported schemas, no-DB source scan; 1 uiTokenPurity gate extended to SecurityTestingPanels.tsx). Suite 1310 -> 1405 (68 files), all green; tsc 2041 (0 new in touched files - pre-existing SecurityTesting.tsx getTests/getComplianceHealth/scheduleTest + routers.ts backlog baseline unchanged); smoke green; committed to dev (e3ac441). Conductor reconciled 3-way parallel-agent contract drift to the engine contract (byStatus/byType -> countsByStatus/countsByType camel keys, red-team `exercise` wrapper -> top-level input, dueAt -> estimatedEndAt, `class` -> assetClass, result/title -> status/controlName, planned -> pending, IG1/Govern -> ig1/govern, byClass object map -> rollup array).

### Cycle 20 - NIS2 security metrics & reporting: MTTR, vulnerability age, compliance drift & executive summary (NIS2 plan Phase 4 Task 4.2) (2026-08-19)
- Backend lib/nis2/securityMetrics.ts: pure deterministic NIS2 security metrics engine (zero deps, never throws, injectable clock) - computeMttr (only rows with valid detectedAt + resolvedAt and resolvedAt>=detectedAt count; per-severity buckets critical..unknown in deterministic order + overall rollup {mttrHours/minHours/maxHours 1 decimal, mttrDays 2 decimals, count}; totalResolved; malformed -> EMPTY_MTTR_RESULT, never throws), trackVulnerabilityAge (whole-day ages vs injected clock; terminal statuses patched/risk-accepted/false-positive excluded case-insensitively; overdue thresholds critical 30d/high 60d/medium 90d/low 120d/info 180d/unknown 180d, strictly > threshold; overall applies each row's own severity threshold; age-band counts 0-7/8-30/31-60/61-90/90+; avgAgeDays 1 decimal + maxAgeDays; deterministic id-asc tie-break), detectComplianceDrift (scores clamped 0-100, driftPts = current-baseline 1 decimal; alert bands <= -20 critical / <= -10 high / <= -5 medium / <= -0.05 low, improved >= 0.05 else stable; catalog recommendations per severity, '' for stable/improved; dedup by areaId last-wins; alerts sorted driftPts asc then areaId asc; counts + totalAlerts + totalAreas), buildExecutiveSummary (postureScore clamped 0-100 1 decimal; counts whole non-negative; MTTR trend vs prior pctChange = (prior-current)/prior*100 1 decimal, improved/worsened/flat or n-a when prior missing/0; status bands Critical when driftAlertCount>=5 OR overdueVulns>=10 OR postureScore<50, Watch when >=1/>=1/<75, else Good; topAreasAtRisk dedup + driftPts asc + areaId asc sliced to 3; non-object -> EMPTY_EXECUTIVE_SUMMARY). Complements lib/nis2/vulnerabilityMgmt.ts (cycle 19) - same house pattern.
- Backend server/routers/securityMetrics.ts: createSecurityMetricsRouter(t, protectedProcedure) - protected pure mttr / vulnerabilityAge / complianceDrift / executiveSummary queries with exported zod schemas (securityMetricsMttrInputSchema / securityMetricsVulnerabilityAgeInputSchema / securityMetricsComplianceDriftInputSchema / securityMetricsExecutiveSummaryInputSchema + item schemas, shared clockOptionSchema now string|number / clock fn, loose nullish fields with engine coercion); zod BAD_REQUEST only, no DB access; wired as `securityMetrics:` in routers.ts (2 lines).
- UI: pages/securityMetricsApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_MTTR / EMPTY_VULNERABILITY_AGE / EMPTY_COMPLIANCE_DRIFT / EMPTY_EXECUTIVE_SUMMARY, retry:false hooks useMttr/useVulnerabilityAge/useComplianceDrift/useExecutiveSummary, MTTR_SEVERITY_META / DRIFT_SEVERITY_META / AGE_BAND_META / TREND_META / POSTURE_STATUS_META + trendGlyph(inverted) / formatMetricHours helpers, DEMO_MTTR_INCIDENTS / DEMO_VULNERABILITY_ITEMS / DEMO_DRIFT_AREAS + buildDemoMttr/VulnerabilityAge/ComplianceDrift/ExecutiveSummary per sec.17 with fixed demo clock); pages/cyber/SecurityMetricsPanels.tsx "NIS2 Security Metrics & Reporting" section on CyberMonitoring (executive summary strip with posture score + status badge + trend arrows; MTTR per-severity table with hours/min/max + overall; vulnerability age table with avg/max/overdue + age-band chips; compliance drift alert cards with severity badges + recommendations + stable/improved counters; demo-mode amber banner; Skeleton 1:1 + EmptyState "Connect the securityMetrics.<proc> API" degradation; token-only, dark-mode safe); CyberMonitoring.tsx additive embed after the CVE & vulnerability cards.
- QA: +64 tests (50 engine incl. MTTR aggregation math + 1/2-decimal rounding + zero-duration rows + bucket order + malformed/determinism/never-throws, age clock injection ISO/epoch/Date/factory/throwing-factory, floor age math, band boundaries, overdue strict-boundary 30/31 60/61 90/91 120/121 180/181 per severity + overall per-row thresholds, terminal exclusion case-insensitive, avg/max math, drift band boundaries -20/-19.9 -10/-9.5 -5/-4.5 + stable/improved edges, score clamping, dedup last-wins + sort, exec-summary score/count coercion, MTTR trend math, status band boundaries, top-areas dedup/sort/slice-3; 14 router contract incl. route shape/no-mutations, protected UNAUTHORIZED rejection, zod BAD_REQUEST on type mismatches (Date-object timestamps rejected, loose enum strings engine-coerced), clock options validation, exported schemas, no-DB source scan). Suite 1246 -> 1310 (66 files), all green; tsc 2041 (0 new in touched files; only pre-existing trpc.threatIntel.* + routers.ts backlog baseline); smoke green; committed to dev (42548dd).

### Cycle 19 - NIS2 CVE & vulnerability tracking: classification, patch prioritization, zero-day triage & register summary (NIS2 plan Phase 4 Task 4.1) (2026-08-19)
- Backend lib/nis2/vulnerabilityMgmt.ts: pure deterministic NIS2 vulnerability engine (zero deps, never throws, injectable clock) - classifyVulnerability (CVSS severity bands >=9 critical / >=7 high / >=4 medium / >0 low / else info, cvssBand range label + 'unknown' on null/NaN; cveId normalized to uppercase CVE-YYYY-NNNN+ else null; isKev/assetCriticality coerced, invalid criticality -> unknown; stable deduped reason list capped 5), prioritizePatch (score 0-100 clamped = severityBase critical40/high30/medium15/low5/info0 + KEV25 + assetCriticalityBase critical15/high10/medium5/low0/unknown0 + exploitabilityBase active20/weaponized15/proof-of-concept8/none0/unknown0; priority P0 when KEV else score>=80 P0 / >=60 P1 / >=40 P2 / else P3; remediationSlaDays P0 7 (KEV 1) / P1 30 / P2 90 / P3 180 with slaDays override; targetDate from injected clock; rationale capped 5), assessZeroDay (isZeroDay = KEV or no-cveId with high/critical; verdict immediate-action 24h / escalate 72h / monitor 168h / informational (null SLA+deadline); verdict-specific requiredActions catalog capped 5), getVulnerabilityRegisterSummary (status/severity aggregation incl. kebab->camel status keys; overdue = dueAt<now, due-soon = 0<daysUntilDue<=7, terminal states patched/risk-accepted/false-positive -> 'n-a' and excluded from overdue/due-soon; patchComplianceRate = patched/(open+inProgress+patchScheduled+patched+riskAccepted) 1 decimal, 0 on zero denominator; per-row enrichment daysOpen/daysUntilDue/slaStatus; deterministic sort severity desc -> dueAt asc (nulls last) -> cveId asc -> id asc; malformed input -> zeroed summary, never throws). Complements lib/cyber/incidentClassifier.ts + lib/nis2/supplyChain.ts + thirdPartyRisk.ts.
- Backend server/routers/vulnerabilityMgmt.ts: createVulnerabilityMgmtRouter(t, protectedProcedure) - protected pure classify / prioritize / zeroDay / registerSummary queries with exported zod schemas (vulnerabilityClassifyInputSchema / vulnerabilityPrioritizeInputSchema / vulnerabilityZeroDayInputSchema / vulnerabilityRegisterItemSchema + vulnerabilityRegisterSummaryInputSchema, loose nullish fields with engine coercion); zod BAD_REQUEST only, no DB access; wired as `vulnerabilityMgmt:` in routers.ts (2 lines).
- UI: pages/vulnerabilityMgmtApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_VULNERABILITY_REGISTER_SUMMARY / EMPTY_ZERO_DAY_ASSESSMENT, retry:false hooks useVulnerabilityClassify/Prioritize/ZeroDay/RegisterSummary, SEVERITY_META / PRIORITY_META / VULN_STATUS_META / SLA_STATUS_META / ZERO_DAY_VERDICT_META + complianceBarClass/formatDaysUntilDue/formatSlaCountdown helpers, DEMO_VULNERABILITY_REGISTER_ITEMS incl. Log4Shell/EternalBlue/MOVEit/PAN-OS KEVs + buildDemoVulnerabilityRegister/Input/ZeroDayAssessment per sec.17 with fixed demo clock); pages/cyber/VulnerabilityRegisterPanels.tsx "Vulnerability Register" tab on VulnerabilityManagement (4 stat cards: open / overdue / patch compliance rate w/ progress / KEV count; Zero-day response panel with verdict-colored accent + severity/verdict badges + SLA countdown + required-actions checklist + rationale; density register table CVE+KEV badge / asset / severity / priority / status / due countdown + slaStatus badge / days open with overdue/due-soon accents; demo-mode amber banner "Demo mode: showing sample data."; Skeleton 1:1 + EmptyState "Connect the vulnerabilityMgmt.<proc> API" degradation; token-only, dark-mode safe); VulnerabilityManagement.tsx additive embed replacing only the empty register placeholder (CVE Intelligence tab untouched).
- QA: +98 tests (78 engine incl. CVSS band boundaries 9/8.9, 7/6.9, 4/3.9, 0.1/0/null, cveId normalization, KEV/asset coercion, full score weight math + clamp/floor, priority edges + KEV->P0 override, severity fallback from cvssScore, slaDays override + injected-clock ISO target dates, isZeroDay rules, verdict mapping + 24/72/168h windows, status/severity aggregation, SLA windows 7/8-day edges + 0-day + past, terminal-state exclusion, compliance rate 0-denominator + 1-decimal, deterministic sorting + enrichment, malformed/determinism/never-throws + clock injection; 20 router contract incl. route shape/no-mutations, protected UNAUTHORIZED rejection, zod BAD_REQUEST on type mismatches, exported schemas, no-DB source scan). Suite 1148 -> 1246 (64 files), all green; tsc 2041 (0 new in touched files; only pre-existing trpc.threatIntel.* baseline in the untouched CVE Intelligence tab); smoke green; committed to dev (f7b2d18).

### Cycle 18 - NIS2 third-party risk scoring: composite supplier risk, certificate tracking & supply chain map (NIS2 plan Phase 3 Task 3.2) (2026-08-19)
- Backend lib/nis2/thirdPartyRisk.ts: pure deterministic third-party risk scoring engine (zero deps, never throws, injectable clock) - aggregateSupplierRisk (composite 0-100 = 0.4 x criticalityNumeric from classifySupplierCriticality band map critical=90/high=70/medium=50/low=30/unknown=0 + 0.4 x posture score from scoreSupplierSecurityPosture (No Data -> 0) + 0.2 x residual (explicit vendorResidualScore or computeVendorRiskTier derivation from vendor signals; redistributed 50/50 when absent); risk tier derived from the UNROUNDED weighted sum, bands critical>=75/high>=55/medium>=30/low<30 - conductor-fixed so 74.99 stays high / 54.99 medium / 29.99 low; verdict + deduped recommendedActions capped at 5; totals count + per-tier counts + avgCompositeScore 1 decimal; malformed input -> zeroed totals, never throws), trackSupplierCertificate (CERTIFICATE_TYPES catalog: ISO 27001 / SOC 2 Type I-II / PCI DSS v4 / HIPAA / GDPR / Cyber Essentials / NIST CSF; statuses valid / expiring (validTo-now <= 90d) / expired vs injected clock + missing rows for cert-less suppliers; signed daysUntilExpiry; summary total/valid/expiring/expired + coverageRate = suppliers with >=1 valid-or-expiring cert / total, 3 decimals), buildSupplyChainMap (nodes sorted composite desc then name asc; edges default weight by source tier critical 1.0 / high 0.8 / medium 0.5 / low 0.3 with explicit override; summary tierCounts + avgCompositeScore + edgeCount). Complements cycle-17 supplyChain.ts lifecycle + cycle-11 vendorRisk.ts tiers.
- Backend server/routers/thirdPartyRisk.ts: createThirdPartyRiskRouter(t, protectedProcedure) - protected pure aggregateRisk / certificates / supplyChainMap queries with exported zod schemas (thirdPartyAggregateRiskSchema / thirdPartyCertificatesSchema / thirdPartySupplyChainMapSchema + sub-schemas); zod BAD_REQUEST only, no DB access; wired as `thirdPartyRisk:` in routers.ts (2 lines).
- UI: pages/thirdPartyRiskApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_AGGREGATE_RISK / EMPTY_CERTIFICATES / EMPTY_SUPPLY_CHAIN_MAP, retry:false hooks useAggregateRiskQuery/useCertificatesQuery/useSupplyChainMapQuery + combined, RISK_TIER_META / CERT_STATUS_META / VERDICT_META helpers, DEMO_TPR_SUPPLIERS / DEMO_TPR_DEPENDENCIES / DEMO_TPR_CERTIFICATE_SPECS + buildDemoAggregateRisk/Certificates/SupplyChainMap per sec.17); pages/cyber/ThirdPartyRiskPanels.tsx "Third-Party Risk Scoring" section on SupplyChainRisk (AggregatedRiskScorecard: 6 totals tiles + per-supplier composite score bars + tier/verdict badges + top recommended actions; CertificateTracker: status-summary tiles + table with status badges, expiry countdown (Nd left/ago), coverage-gap rows for missing certs; SupplyChainMapSummary: nodes/edges/avg-score tiles + tier distribution bars (data-viz sec.18) + weighted dependency highlights; demo-mode amber banner; Skeleton 1:1 + EmptyState "Connect the thirdPartyRisk.<proc> API" degradation; token-only, dark-mode safe); SupplyChainRisk.tsx additive embed after the lifecycle section.
- QA: +46 tests (31 engine incl. band boundaries on unrounded sums (75/74.99, 55/54.99, 30/29.99), weight math 40-40-20 + 50-50 redistribution, rounding + 1-decimal averages, malformed/undefined -> zeroed no-throw, determinism, clock injection, certificate 90-day window + missing/coverage math, map ordering + tierCounts + default/override edge weights; 15 router contract incl. protected UNAUTHORIZED rejection, zod BAD_REQUEST on invalid input, exported schemas, no-DB assertion). QA surfaced 1 source bug (tier computed from rounded composite) - conductor fixed in-engine (tier from unrounded sum) and corrected 1 test expectation (raw 74.99 -> high per band spec). Suite 1102 -> 1148 (62 files), all green; tsc 2041 (0 new in touched files; only pre-existing routers.ts backlog shifted by the 2 wiring lines); smoke green; pushed to origin/dev + github/dev.

### Cycle 17 - NIS2 supplier security lifecycle: criticality, posture, incident SLA & monitoring SLA (NIS2 plan Phase 3 Task 3.1) (2026-08-19)
- Backend lib/nis2/supplyChain.ts: pure deterministic NIS2 supply-chain engine (zero deps, never throws, injectable clock) - classifySupplierCriticality (additive 0-100 criticality: essential services +45 / sensitive data +30 / broad network +25 / restricted +15 / subcontractor -10 / spend >= EUR 1M +10, clamped; bands critical>=70/high>=50/medium>=25; review cadence Quarterly/Semi-Annual/Annual/Biennial + nextReviewDate from injected clock; stable rationale, fallback "No criticality factors identified"), scoreSupplierSecurityPosture (8 weighted contractual controls summing to 100: security requirements 20 / incident notification 15 / vulnerability handling 15 / audit rights 12 / subcontracting 10 / termination-exit 10 / data protection 10 / business continuity 8; readiness Strong>=85/Developing>=65/At Risk below/No Data when no answers; gaps in catalog order + gap-derived recommendedActions capped at 5), trackSupplierIncident (notification SLA default 24h significant / 72h else, custom slaHours honoured; deadline = detectedAt + SLA; status precedence acknowledged > submitted > overdue > due (12h window) > pending vs injected clock; signed daysRemaining, null once reported/acknowledged; deterministic kebab incidentId slug, untitled-incident fallback), monitorSecuritySla (cadence-driven statuses: not-tested / breached >2x / at-risk >1x / compliant; daysUntilDue signed; invalid items filtered, zeroed summary on missing input). Complements (does not replace) lib/vendor/vendorRisk.ts TPRM tiers.
- Backend server/routers/supplyChain.ts: createSupplyChainRouter(t, protectedProcedure) - protected pure classifySupplier / scorePosture / trackIncident / monitorSla queries with exported zod schemas (supplyChainClassifySchema / Posture / Incident / Sla); zod BAD_REQUEST only, no DB access; wired as `supplyChain:` in routers.ts (2 lines).
- UI: pages/supplyChainApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_SUPPLIER_CLASSIFICATION / EMPTY_POSTURE_SCORE / EMPTY_INCIDENT_NOTIFICATION / EMPTY_SLA_MONITOR, retry:false hooks useClassifySupplier/useScorePosture/useTrackIncident/useMonitorSla, SUPPLIER_CRITICALITY_META / POSTURE_READINESS_META / INCIDENT_STATUS_META / SLA_STATUS_META + POSTURE_QUESTION_LABELS/ORDER, DEMO_SUPPLIER_SLA_ITEMS + buildDemoSlaMonitor per sec.17); pages/cyber/SupplyChainLifecyclePanels.tsx "NIS2 Supplier Security Lifecycle" section on SupplyChainRisk (criticality classifier with score bar + rationale + review date; 8-question posture scorecard with readiness badge + gap chips + recommended actions; incident notification tracker with deadline + status badge + days remaining; demo-mode SLA monitor table with amber banner + summary counts; Skeleton/EmptyState degradation, token-only, dark-mode safe); SupplyChainRisk.tsx converted to token-only colors (0 forbidden slate/sky/emerald/amber surface refs, data-viz/token exceptions kept).
- QA: +79 tests (61 engine incl. band boundaries 70/50/25, factor sums + clamp, cadence dates 3/6/12/24mo, readiness bands + No Data vs data-present, status precedence + 12h due window + custom/invalid SLA hours, daysRemaining sign, slug sanitization, invalid-item filtering, determinism + never-throws; 18 router contract incl. route shape/no-mutations, zod BAD_REQUEST per schema, degenerate-but-valid inputs, no-DB assertion, exported schemas) - suite 1023 -> 1102 (60 files), all green; tsc 2041 (0 new in touched files; only pre-existing routers.ts backlog shifted by the 2 wiring lines); smoke green; pushed to origin/dev + github/dev.

- Backend lib/cyber/incidentTimeline.ts: pure deterministic incident timeline & escalation engine (zero deps, never throws, injectable clock) - buildIncidentTimeline (4 ordered phases Detection -> Early warning -> Incident notification -> Final report with deadline anchors reused from incidentClassifier.getReportingDeadlines (24h/72h/30d) and completed/current/upcoming lifecycle + isComplete; malformed input -> EMPTY_INCIDENT_TIMELINE), computeEscalations (deterministic ordered triggers: monitor (info) below Art. 23(3) significance; sev-escalation (warning) for high/critical significant incidents before notification; ew/notif/final-due (warning) inside the 12h window; ew/notif/final-overdue (critical) with dueBy; complete (info) when all reports sent; never throws, no duplicate ids, stable order).
- Backend server/routers/incidentTimeline.ts: createIncidentTimelineRouter(t, protectedProcedure) - protected pure timeline / escalations queries sharing the exported incidentTimelineInputSchema (zod BAD_REQUEST only, no DB access); wired as `incidentTimeline:` in routers.ts (2 lines).
- UI: pages/incidentTimelineApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_INCIDENT_TIMELINE/EMPTY_ESCALATIONS, retry:false hooks, INCIDENT_PHASE_META / ESCALATION_LEVEL_META + getPhaseMeta/getEscalationMeta/sortEscalations/isEscalationOverdue); CyberIncidentDetail "NIS2 Incident Timeline" panel (4-step vertical timeline with status badges + current-phase highlight, color-coded escalation list with due-by dates + Overdue badges, Skeleton + EmptyState degradation, hidden without detection date); CyberIncidentReporting critical-escalation callout banner; token-only, dark-mode safe.
- QA: +39 tests (28 engine incl. phase order/labels/deadline math, status transitions, clock injection, malformed-input never-throws, determinism, due-window boundaries + overdue suppression + no-duplicate-id sweeps; 11 router contract incl. zod BAD_REQUEST + no-DB assertion) - suite 984 -> 1023 (58 files), all green; tsc 2041 (0 new in touched files; pre-existing trpc.cyber.* + routers.ts backlog unchanged); smoke green; pushed to origin/dev + github/dev.

### Cycle 15 - NIS2 Art. 23 incident classification & reporting deadlines (NIS2 plan Phase 2 Task 2.1) (2026-08-19)
- Backend lib/cyber/incidentClassifier.ts: pure deterministic NIS2 incident engine (zero deps, never throws) — classifyIncident (Art. 23(3) significance across 7 criteria with stable reasons; additive 0-100 severity score weighted public-safety/critical-infra/data-integrity/cross-border/users/duration/loss, clamped 0-100, bands critical>=80/high>=55/medium>=30; ENISA threat category from ordered cause-keyword map over lib/threat-intel/enisa-taxonomy.ts, fallback Other/TH-000; nextDeadline 24h when significant), getReportingDeadlines (24h/72h/30d milestones with pending/due/overdue status vs injectable clock, 12h due window), buildCsirtTemplate (plain-text NIS2 notification addressed to the EU competent authority from lib/nis2/competent-authorities.ts; generic fallback, never throws).
- Backend server/routers/incidentClassifier.ts: createIncidentClassifierRouter(t, protectedProcedure) — protected pure classify / deadlines / csirtTemplate queries (zod BAD_REQUEST only, no DB access); wired as `incidentClassifier:` in routers.ts.
- UI: pages/incidentClassifierApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false hooks, getSeverityMeta/getDeadlineMeta/getNextDeadlineLabel helpers); CyberIncidentDetail "NIS2 Classification" panel (severity badge + score bar, significance verdict + reasons list, ENISA category chip, deadline tracker with status badges, CSIRT template preview with EU country select, Skeleton + EmptyState degradation); CyberIncidentReporting quick classification callout; token-only, dark-mode safe.
- QA: +92 tests (80 engine incl. threshold boundaries, malformed input, determinism, clock injection, template variants; 12 router contract incl. zod BAD_REQUEST + no-DB) — suite 892 -> 984 (56 files), all green; tsc 2041 (0 new in touched files; 3 pre-existing trpc.cyber.* errors unchanged vs HEAD); smoke green; pushed to origin/dev + github/dev.

### Cycle 14 - AI compliance copilot productized (scorecard #10 done) (2026-08-19)
- Backend lib/ai/copilot.ts: pure deterministic copilot engine (zero deps, never throws) - draftPolicyDraft (10 topic templates + generic fallback, framework-specific controls, orgName surfaced in title + scope), suggestEvidenceForControl (evidence catalog with freshness hints, stable fallback), autoMapRequirement (0-100 keyword scoring, ranked matches + bestMatch) over a built-in control KB for SOC 2 / ISO 27001 / NIS2 / GDPR / HIPAA / PCI DSS. Tiered keyword matching (verbatim phrase 1.0 > all-token 0.7 > single-token 0.35) fixes 'access control' -> remote-work template mis-selection.
- Backend server/routers/aiCopilot.ts: createAiCopilotRouter(t, protectedProcedure, publicProcedure) factory - protected draftPolicy / suggestEvidence / autoMap mutations (zod BAD_REQUEST only throw, no DB access) + public deterministic status probe; wired as `aiCopilot:` in routers.ts.
- UI: pages/aiCopilotApi.ts typed contract layer (UI-STANDARD sec.16 cast pattern, retry:false hooks, EMPTY_AI_COPILOT_STATUS); AgentPage "Compliance copilot" quick-actions (draft policy / suggest evidence / auto-map cards, token-only, Skeleton loading + EmptyState degradation when endpoint not live).
- QA: +33 tests (22 engine unit incl. framework-distinct controls, determinism, no-throw; 11 router contract incl. zod BAD_REQUEST + no-DB status) - suite 859 -> 892 (54 files), all green; tsc 0 new errors in touched files (routers.ts 55 = 55 vs HEAD); smoke green.
- Cycle-13 follow-up committed: AuditManager converted to auditApi contract layer; trustCenter registered with publicProcedure (public posture page semantics, matches createTrustCenterRouter signature).

### Cycle 13 - Trust center + audit management parity (scorecard #8/#9 done) (2026-08-19)
- Backend server/routers/trustCenter.ts: hardened all public procedures with graceful DB degradation (never throw on DB failure - neutral/empty shapes); new public `getPosture` procedure (client profile + latest complianceSnapshot score/status Strong/Developing/At Risk/No Data + implemented/total controls + trust documents; empty neutral shape on DB failure/unknown client); competitor block preserved.
- Backend lib/reporting/auditPackageGenerator.ts: extracted pure helpers (computePassRate, buildControlsCsv, buildAuditManifestJson) + structured AuditPackageError (DB_UNAVAILABLE/CLIENT_NOT_FOUND/GENERATION_FAILED); server/routers/auditPackage.ts translates to TRPCError (NOT_FOUND/INTERNAL_SERVER_ERROR).
- UI: token-only conversion of TrustCenter.tsx / AuditHub.tsx / AuditManager.tsx per UI-STANDARD sec 2 (592 -> 0 hard-coded slate/gray/white/indigo surface refs; data-viz exceptions retained with dark: variants; invalid /100/ opacity chains fixed; ring/divide slate -> tokens); TrustCenter now surfaces trustCenter.getPosture (score badge + status + implemented/total) with EmptyState/Skeleton degradation; new typed contract layers pages/trustCenterApi.ts + pages/auditApi.ts (UI-STANDARD sec 16 cast pattern, 0 new tsc errors).
- QA: +34 tests (trustCenterRouter 21 contract/degradation tests, auditPackageGenerator 10 pure-helper tests, uiTokenPurity 3 static gate tests); suite 825 -> 859 (52 files); tsc 2043 -> 2043 (0 new in touched files); smoke green.
### Cycle 12 - Questionnaires & surveys auto-scoring (scorecard #6 done) (2026-08-19)
- Backend lib/questionnaire/questionnaireScoring.ts: deterministic pure scoring engine - per-answer classification (pass/partial/fail/neutral/unanswered), focus-area aggregation with per-area 0-100 scores, completionRate, complianceScore 0-100, readiness levels (Strong/Developing/At Risk/No Data), summarizeScore (label/tone/description); zeroed "No Data" on non-array/malformed input, never throws.
- Backend lib/questionnaire/templates.ts: built-in SIG Lite (~20 questions across Governance/Access Control/Data Protection/Security Testing/Cryptography/Incident Response/Vendor Mgmt/HR) + CCM CAIQ v4 (all 15 CCM domains: AIS/BCR/CCC/CEF/DSI/DCS/GRM/HRS/IVS/LOG/MON/SEF/STA/TVM/UEM); questionCount derived, unique ids, known responseTypes only.
- Router refactored to createQuestionnaireRouter(t, clientProcedure, publicProcedure) factory (test-harness friendly); new `score` procedure (DB-backed, never throws - DB failure -> empty score readiness "No Data", unknown id -> null) and `scoreAnswers` (pure passthrough).
- UI: questionnaireApi.tsx contract layer (UI-STANDARD sec 16: useQuestionnaireScore, ScoreBadge/ScoreProgress/getReadinessMeta/getScoreToneClass/getScoreBarClass); QuestionnairesDashboard per-row Score column (badge + progress, Skeleton while loading, dash on error/empty, colSpan 8); QuestionnaireWorkspace score/readiness surfacing; token-only polish (bg-muted/border-border/bg-card/text-destructive, dark-mode status badges).
- Conductor fixed 1 new tsc error (getScoreBarClass null-acceptance). Known follow-up: neutral answers count toward `answered` (completionRate/complianceScore) - asserted as spec.
- Tests: 72 new (39 scoring incl. readiness-boundary/edge/malformed cases, 19 templates integrity + CCM domain coverage, 14 router incl. zero-rows empty score) - 753 -> 825 (49 files), all green; coverage 100% on the 5 configured targets; tsc 3614 -> 3612 (0 new in touched files).

### Cycle 11 - TPRM per-vendor risk tiering (scorecard #5 done) (2026-08-18)
- Backend lib/vendor/vendorRisk.ts: pure, deterministic per-vendor risk engine - inherent risk by data-access class (PII/ePHI/Infrastructure -> critical), residual score 0-100 (higher = safer) from SOC2 attestation / latest threat-intel scan / open high-critical assessments / contract / DPA / subprocessor signals, Tier 1 (Critical) / Tier 2 (High) / Tier 3 (Medium) mapping at <50/<75, quarterly/semi-annual/annual review cadence + next-review date (injectable clock), 3-5 recommended actions, portfolio overview (tier counts, avg residual score, due-for-review list).
- tRPC vendorRisk router (getOverview / getVendorRisk): client-scoped queries over vendors, scans, assessments, contracts, DPAs (schema-verified); latest-scan selection ignores rows without a risk score (no broken-scan shadowing); Draft contracts no longer count as active; NEVER throws on read - DB failure returns an empty overview; unknown vendor -> NOT_FOUND.
- UI: vendorRiskApi.ts contract layer (UI-STANDARD sec 16); VendorDashboard risk overview (tier stat cards, residual score bars, next-review due list); VendorList per-vendor "Risk Tier" column (tier badge + next-review date, Skeleton while loading, dash on error/empty); VendorDetails "Vendor Risk" card (residual score bar, tier badge, review frequency, next review, recommended actions, EmptyState degradation); token-only dark-mode polish across TPRM pages (VendorOverview/GlobalVendorCatalog/SecurityReviews/SubprocessorRegister/TPRMLayout/DPA empty states).
- Tests: 87 new (54 engine boundaries/signals/overview + 24 lib + 9 router contract w/ mocked db) - 666 -> 753 (46 files), all green; tsc 0 new errors in touched files (0 in all new files).

### Cycle 9 - Enterprise SSO (OIDC + reverse-proxy header auth) (scorecard #13) (2026-08-18)
- Backend lib/sso/oidc.ts: OIDC discovery, authorization-code URL builder, token exchange, RS256 ID-token verification against the IdP JWKS (issuer/audience/exp/iat/nonce checks, node crypto), claim mapping with configurable role claim, single-flight 10-minute state store. All network access injectable-fetch for tests.
- Backend lib/sso/proxy-auth.ts: reverse-proxy header SSO (Traefik/Nginx/Authentik style) - case-insensitive header lookup on configurable principal/email headers, namespaced openId (sso_proxy:<email>).
- tRPC sso router (status/start/callback) wired into routers.ts: start generates state+nonce and returns the IdP authorization URL; callback consumes state single-flight, exchanges the code, verifies the ID token, upserts the user by openId (loginMethod 'sso') and issues a localAuth session JWT. status never echoes secrets.
- authMiddleware: proxy-SSO support - when enabled and no Authorization header, resolves the principal from configured headers and upserts the user (loginMethod 'sso_proxy').
- Config: SSO_OIDC_* / SSO_PROXY_* env vars added to _core/env.ts and documented in .env.example (no secrets).
- UI: ssoApi.ts contract layer (UI-STANDARD sec 16), LoginPage "Continue with SSO" button shown only when sso.status reports OIDC mode, new /auth/sso/callback page (stores session token exactly like local auth, reloads into the app), read-only SSO status card on Security settings (token-only, dark-mode safe).
- Tests: 43 new (30 oidc incl. real RS256/JWKS mocking + negative cases, 10 proxy-auth, 3 router) - 623 -> 666 (43 files), all green; coverage 100% on the 5 configured targets; tsc 2046 -> 2046 (0 new).

### Cycle 8 - Webhook system (scorecard #15) + scorecard reconciliation (2026-08-18)
- Webhook registry hardened: bounded retries with exponential backoff (default 2; 4xx never retried; injectable fetch for tests), updateWebhookSubscription / deleteWebhookSubscription / listWebhookEventCatalog, recursive secret scrubbing on dispatched + logged payloads, and dispatch that never throws (graceful DB degradation, per-write guards).
- New lib/webhooks/webhookEvents.ts: single-source event catalog (test.ping, evidence.expired, control.autotest.failed, risk.created, policy.ack.overdue, "*") + safeDispatchWebhookEvent fire-and-forget dispatcher (setImmediate, never blocks callers).
- Router: webhooks.updateSubscription / deleteSubscription / listEventCatalog added; subscribe/update events validated against the catalog; listDeliveries limit bounded (1-200); TRPCError-wrapped.
- Real event wiring (non-blocking): evidence.expired (renewal loop x3), control.autotest.failed (auto-test engine), policy.ack.overdue (reminder sweep), risk.created (high/critical upsert).
- UI: token-only Webhooks page (/clients/:id/webhooks + /webhooks alias + nav) - stat cards, subscriptions CRUD with enable/disable switch, test-ping trigger, deliveries table, create dialog with one-time secret reveal, per-procedure EmptyState degradation; webhooksApi.ts contract layer per UI-STANDARD sec 16.
- Tests: 565 -> 623 (40 files), all green; coverage 100% on the 5 configured targets; tsc 2046 -> 2046 (0 new; 0 errors in all new files).
- Scorecard reconciliation: TPRM (#5), questionnaires (#6), trust center (#8), audit management (#9), integrations/API (#13) rows updated to reflect existing implementations (previously "None").

### Cycle 7 — GRC cross-module integration, market-readiness hardening, CI (2026-08-17)

**GRC cross-module integration (information now flows between modules):**
- New `lib/grc-integration.ts`: idempotent `findOrCreateTask` (dedupes on related entity + open status) and `notifyClientOnce` (≈daily notification dedupe) shared by all propagation flows.
- Audit findings: high/critical findings auto-create a remediation task on the client task board and notify client users (`findings.create`).
- Control auto-tests: failing scheduled runs emit one deduped summary notification per client per day (scheduler layer; engine stays pure).
- Evidence expiration: each item flipped to `expired` creates a renewal task (deduped) in addition to existing warnings.
- Incidents: high/critical incidents auto-create a draft risk-register entry; continuity-triggered incidents prompt a BC activation assessment; all incidents notify.
- Vendors: completing an assessment with high/critical residual risk updates vendor criticality and upserts a "Vendor risk: <name>" register entry.
- DSAR: new `dsarDeadlineScheduler` (daily) — 7-day warnings + overdue alerts as notifications, overdue DSARs as critical tasks; wired in `server_entry.ts`.
- Unified action center: `actions.listAll` now aggregates risk treatments, open audit findings, and ≤7-day/overdue DSARs alongside POA&Ms/roadmap/project/generic tasks; `updateStatus` supports all types with allowed-value guards; previously-ignored `type`/`assigneeId` filters now applied.

**Fixed broken integrations (schema drift / silent failures):**
- `treatmentControls.updatedAt` written though the column doesn't exist — aborted the Evidence→Control→Treatment→Risk recalculation chain (`evidence.ts`, `risks.ts`).
- Gap→risk lineage: `gapResponseId` sent by the gap UI was stripped by zod; now accepted and persisted (`risks.upsert`).
- `getRiskTreatments` never returned linked controls (write-only linkage); now returns `linkedControls` per treatment.
- Executive metrics always zero: BIA completion rate (wrong columns `criticality`/`biaStatus` → `criticalityTier` + BIA join) and critical-vendor risk (non-existent `vendors.riskScore` → trust-score based).
- Breach split-brain: metrics read the unused `dataBreaches` table while the register UI writes `BREACH:` privacy assessments; metrics now merge both stores.
- Task creation was broken app-wide: `actions.create` inserted non-existent `source_type` column; removed, and `relatedEntityType`/`relatedEntityId` inputs added for entity linking.
- Dead `kris` router mount removed (inline router was the live one; duplicate key shadowed the module).
- Deleted dead-and-broken `scalable-dashboard.ts` (unmounted, referenced non-existent columns).

**Fabricated data eliminated (trust):**
- `dashboard.complianceScores` returned a hardcoded fake improving trend (`20 + i*12 + random`); now real monthly averages from compliance snapshots with live-score fallback. Verified deterministic via API.
- `complianceMonitor.getComplianceScore` layered `Math.random()` onto the score; now deterministic healthy/total ratio.
- Autopilot `trigger` invented per-category stats (×0.3/×0.2/×0.5); now returns real engine outcomes.
- VRM trust-center analysis (deterministic demo generator) is now explicitly labeled: `simulated: true` flag, prefix in stored risk summary, and a visible UI disclaimer.

**Platform / UX:**
- New weekly compliance snapshot scheduler so trend charts populate automatically (skips <6-day-old snapshots).
- Compliance monitor cron (hourly health checks + drift events) — implemented but never scheduled since inception — now wired.
- `/api/version` endpoint (package version, node, env, build type, uptime); `/api/health` reports the real package version.
- AI first-run gating: `llm.status` procedure + `useAiConfigured` hook; Generate-with-AI disabled with a clear hint until a real provider key exists; AI errors surface actionable tRPC messages with an "AI Settings" toast action.
- LLM service: fast-fails on demo placeholder API keys with an actionable message; Anthropic client gets the same browser-detection cloak as OpenAI.
- Auto-test history rows are clickable: drill-in dialog shows per-check findings, "View control" opens the details sheet, "Add to task board" creates a linked remediation task.
- Design tokens: `--color-brand`/`--color-brand-bright` added; 764 hardcoded `#1C4D8D`/`#3ABEF9` arbitrary values swept to tokens across 91 files; shared Tabs restyled to token-based segmented control (theme-aware, dark-mode safe).
- Cookie consent now shows only on public marketing pages (was greet-blocking the app on unmatched routes).
- License cache-miss warning logs once per process instead of every request.
- Hot-path debug logging removed (db.ts, clients/controls routers, report generator, LLM client).
- Link-integrity vitest gate added: every internal link literal is checked against registered routes (caught-class: governance dashboard → nonexistent `/clients/:id/risk-register` 404, fixed with link corrections + redirect aliases for `/policies`, `/assets`, `/clients/:id/risk-register`).
- Production build fixed: three components imported non-existent `../utils/trpc` (escaped typecheck, failed vite build); build script now sets `--max-old-space-size=8192` via cross-env (default heap OOMs); `cross-env` installed as a real devDependency.
- CI pipeline: typecheck + lint (hard gate, 5 `any` warnings fixed) + 524-test suite (env-independent, verified without `.env`) + production build; 30-min timeout, Node 24, npm cache, status badge in README.
- README: correct first-boot credentials (`admin@complianceos.local` + startup-log password / `COMPLIANCE_ADMIN_PASSWORD`), dev quickstart (ports, no-hot-reload note), canonical Docker deploy path.
- Tests: 482 â†’ 524 (33 files), all green; typecheck clean.
- Access review automation (scorecard P2 #7): access_review_cycles/tasks schema + migration 0022, `accessReviews` lib (idempotent per-user Ã— role provisioning, certify/revoke with notes + reviewer, overdue sweep with injected clock, summary/history; DB fallback), 9-procedure tRPC router, 12h overdue scheduler, and a token-only AccessReviews page (stat cards, expandable cycles, certify/revoke workflow, history feed, contract layer with graceful EmptyState degradation).
- Verified final: tests 524 â†’ 565 (37 files) all green, coverage 100% on the 5 configured targets, tsc 2045 â†’ 2041 (0 new errors; 7 new errors the feature batch introduced were fixed â€” vendorAssessments duplicate import, msspGovernanceService clientSummaries typing); MsspPartnerPortal token-only pass; internal-links gate green.
- Follow-up (same cycle): global search extended to risk / vendor / task / audit_finding (new db.globalSearch sections + routers type enum + GlobalSearch icons/labels, duplicate switch cases deduped); tsc fixes â€” mui-theme Shadows tuple completed (25th entry), App.tsx unused Theme import removed, tRPC transformer log message stringified. tsc unique signatures 1619 -> 1612 (0 new vs HEAD).

### Cycle 6 - Evidence renewal + policy ack assignment/reminders + framework library (2026-08-14)
- Scorecard #14 (evidence expiration & renewal) completed: renewal loop with auto-remediation.
  - Backend: `evidenceRenewal` lib (due-for-renewal horizon via `buildDueForRenewalWhere`, `getRenewalStateSummary`, `runEvidenceRenewal` with renewed/expired/skipped/failed counts + remediation notes), tRPC router (`evidenceRenewal.getSummary`/`runNow`, zod-validated) wired into `routers.ts`, scheduler wired in `server_entry.ts` (guarded by `ENABLE_EVIDENCE_RENEWAL_SCHEDULER`).
  - UI: `EvidenceRenewalPanel` on the Evidence page - live summary when the endpoint is up, graceful fallback to `evidenceExpiry.getStats` with a "Derived from workspace data" badge (UI-STANDARD §16); token-only, dark-mode safe.
- Scorecard #4 (policy management + ack) completed: employee assignment + reminders.
  - `assignPolicy` (idempotent; explicit userIds or all client-linked users via `user_clients`), `listOverdueAcks`/`selectAcksDueForReminder`/`runPolicyAckReminders` (3-day threshold), `policyAck.assign` procedure, `policyAckReminderScheduler` (12h interval, `ENABLE_POLICY_ACK_REMINDERS`), `PolicyAcknowledgmentPanel` rewrite with assignment UI.
- Scorecard #11 (framework coverage) completed: SOC 2 / PCI DSS v4 / HIPAA / SOX control manifests (`frameworkSeed` + `applyFrameworkControls`) + presentational `FrameworkLibraryPanel` on the Frameworks Dashboard.
- Risk heat map: `normalizeSummaryStatus` unifies DB (planned/in_progress/implemented/verified) and UI (open/in-progress/mitigated/accepted) treatment status vocabularies.
- Tests: 412 -> 482 (30 files), all green; coverage 100% on the 5 configured targets; tsc 2044 -> 2041 (0 new errors, 3 fixed in touched files).

### Cycle 5 - Collector connections + credential management + UI triggers (2026-08-14)
- Scorecard #1 (automated evidence collection) completed: collector connections + credential management + UI triggers.
  - Backend: `evidenceCollectorConnections` lib (masked credentials, secrets never returned; provider manifests from the evidence registry; DB-backed with in-memory fallback; idempotent self-registration of the five built-in collectors) + `evidenceCollectors` tRPC router (`listProviders`/`list`/`save`/`remove`/`test`/`run`) wired into `routers.ts`.
  - UI: `CollectorConnectionsPanel` on the Evidence page replaces the static strip - per-provider status chips, dynamic credential form rendered from manifests (password/select/number/boolean), Test connection, Run-now trigger with live evidence summary, delete with confirm; token-only per UI-STANDARD, dark-mode safe.
  - Tests: 374 → 412 (28 files), all green. tsc: no new errors in touched files.

### Cycle 4 - Cloud evidence collectors + per-client auto-test schedules (2026-08-14)
- Shipped AWS / Azure / GCP automated evidence collectors (scorecard #1): three new manifest-driven, injectable-fetch collectors registered in the evidence registry (`aws`, `azure`, `gcp`).
  - AWS: `aws.iam-access-key-age` (90d rotation, 60d warning), `aws.s3-bucket-encryption`, `aws.ec2-public-ports`, `aws.cloudtrail-enabled`.
  - Azure: `azure.mfa-status`, `azure.defender-plan-status`, `azure.storage-encryption`, `azure.sql-auditing-enabled`.
  - GCP: `gcp.gcs-bucket-public-access`, `gcp.iam-sa-key-rotation`, `gcp.compute-disk-encryption`, `gcp.cloudsql-ssl-required`.
  - Each collector degrades gracefully (missing credentials / malformed payloads -> `error` evidence, never throws), honors injected clock / TTL / limit, and exports `create{Cloud}EvidenceCollector(fetch?)` for testability.
- Shipped per-client control auto-test schedules (scorecard #2): `client_auto_test_schedules` table + drizzle schema, `isAutoTestDue` / `getClientAutoTestSchedule` / `touchClientAutoTestRun` / `setClientAutoTestSchedule` in the auto-test engine, scheduler skips disabled/not-due clients and records last run, and `controlMonitoring.getScheduleConfig` / `updateScheduleConfig` (intervalHours 1-168, zod-validated).
- UI: Auto-test schedule panel on the Controls page (enable toggle, 1/6/12/24h interval, last-run display) + an "Automated evidence sources" strip on the Evidence page; all token-only per UI-STANDARD.md.
- Tests: 271 -> 374 across 26 files (cloud collector suites + schedule config + router validation), 100% coverage on the 5 configured targets; `npx tsc` adds zero new errors in touched files.


### Cycle 3 — Risk heat map, policy ack, real-time dashboard (2026-08-14)
- Shipped risk heat map + treatment plans (scorecard #3): 5x5 likelihood x impact matrix, per-cell drill-down via `riskHeatmap.listTreatmentPlans`, treatment summaries and forward-only status transitions, new `/clients/:id/risks/heatmap` route.
- Shipped policy acknowledgment workflow (scorecard #4): `policy_acknowledgements` table, `policyAck` router (list / listPending / acknowledge-by-id / listForPolicy / summary) and a PolicyAcknowledgmentPanel on the Client Policies page.
- Shipped real-time dashboard stats (scorecard #12): `dashboard.getStats` (posture score, framework pass rates, evidence coverage, 30-day trend) with a Vanta-style PostureSummary on the Dashboard (falls back to derived stats from `dashboard.enhanced`).
- P0 #2: added a Control Auto-Tests section to the Controls page (run-all trigger + recent run history via `controlMonitoring`).
- Backend aligned to the UI-STANDARD section-16 data contract (riskHeatmap.listTreatmentPlans, policyAck.list/acknowledge, dashboard.getStats).
- Fixed smoke test: Prowler runner gained a `mock` option; `npm run smoke` no longer fires a live Docker scan with placeholder credentials.
- Tests: 192 -> 271 across 18 files (3 new contract suites + evidence-collector regressions), 100% coverage on the 5 configured targets.

### Continuous Control Monitoring (P0)
- Wired the control auto-test engine into tRPC via a new controlMonitoring router (runControlAutoTest, runAllForClient, history) mounted on the app router.
- Added an offline guard to the control auto-test scheduler so an unreachable/non-local database logs once and skips instead of spamming TLS errors.
- Added dbUrl helper (resolveDatabaseUrl, createSafePgOptions) used by the addon init path and schedulers for safe local/remote connection handling.

### Automated Evidence Collection (P0)
- Added an evidence collection scheduler (evidenceScheduler.ts) that runs due evidence rows through the collector registry and persists collected artifacts.
- Added a generic HTTP/API evidence collector (manifest-driven, injectable fetcher, JSON payload normalization) alongside the existing GitHub collector.

### User Experience
- Added reusable PageHeader and StatCard UI components; migrated Controls, Evidence, Login, Security, Vendor List, and Client Policies pages to token-based styling with dark-mode support.
- Added primary-cta / primary-cta-hover color tokens (brand navy) for the default button variant.

### Testing
- Added unit tests: control auto-test engine (16), evidence scheduler (6), HTTP/API collector (13), plus edge cases for the cache manager and evidence collector. Suite: 192 tests across 12 files, 100% coverage on the 5 configured targets.

### Reliability
- Standardized tRPC server error logging with a correlation `errorId`, and sanitized internal error messages in production builds.
- Removed unsafe database URL logging from DB initialization while preserving useful connection lifecycle logging.
- Improved environment health-check logging to use structured server logging.

### Performance
- Fixed cache key generation for API response caching and corrected cache error-rate calculations.
- Optimized in-memory cache maintenance by adding LRU behavior and throttling expiration scans to reduce per-write overhead.
- Added runnable benchmark and load-test scripts under `scripts/performance/` to measure critical-path improvements.

### User Experience
- Improved Clients page reliability by removing noisy debug logs, adding a retryable error empty state, and validating required input before create.

### Testing
- Repaired root test imports by adding thin re-export entrypoints for `db`, `routers`, and `TrpcContext`.
- Added unit tests for business-logic utilities with edge-case coverage.
- Added coverage tooling and a `test:coverage` script, enforcing >=90% coverage for the targeted business-logic modules.



