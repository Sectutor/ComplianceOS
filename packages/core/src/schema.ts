/**
 * Master Schema Entrypoint for ComplianceOS
 * 
 * Re-exports the modular domain schemas from ./schema/index
 * for 100% backward compatibility with existing imports.
 */

export * from './schema/index';
