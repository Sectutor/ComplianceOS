# VANTA-SCORECARD — ComplianceOS Parity Tracker

> Definition of done for the continuous build pipeline. Source: `docs/vanta-feature-map.md`.
> Status: ⬜ Not started | 🟨 In progress | 🟩 Done | 🔒 Blocked
> Last updated: 2026-08-21 (cycle 26 complete - NIS2 Risk Quantification Engine done, NIS2 plan Phase 1 Task 1.2)

## Cycle log
- **Cycle 27**: QA hardening pass over cycle 26's risk quantification (landed in parallel mid-session by a second conductor; already pushed) - uiTokenPurity static gate PAGES list extended to strictly cover pages/riskQuantificationApi.ts + pages/cyber/RiskQuantificationPanels.tsx (+2 assertions, zero forbidden tokens found); independent re-verification: suite 1891 -> 1893 green (78 files), tsc 2033 baseline unchanged (0 errors in any riskQuantification.* file), smoke green; BACKEND/UI/QA review agents found no defects, no source changes required.
- **Cycle 26** (96285ae): NIS2 risk quantification engine (NIS2 plan Phase 1 Task 1.2, ENISA Measure 2.1 "Risk Management Framework" / Art. 21(2)(a)) - new pure deterministic `lib/nis2/riskQuantification.ts` engine (zero deps, never throws, injectable clock): `computeRiskAppetite` (score = L x I 1-25 from numbers or scale keywords rare..almost-certain / negligible..severe, out-of-range numbers and unknown words INVALID not clamped; verdicts within/tolerance/exceeded STRICT > vs appetiteScore default 12 + tolerancePct default 10, exceeded threshold appetite x (1+tol/100); optional EUR annual-loss appetite check; avgScore 1dp; appetiteUtilizationPct clamped 0-100; breaches score desc then id asc capped 5; recommendations capped 5 fixed order omitting zero buckets, standing review rec only when assessedCount > 0; totalRisks counts object rows incl. garbage; malformed -> EMPTY_RISK_APPETITE), `buildQuantitativeMatrix` (fixed 25-cell grid likelihood 5..1 x impact 1..5 with per-cell counts; bands low 1-4 / medium 5-9 / high 10-15 / critical 16-25; byBand; avgScore 1dp; topRisks capped 5 score desc then id asc numeric-before-string; malformed -> EMPTY_RISK_MATRIX with empty grid), `trackResidualRisk` (inherent = inherentScore override else LxI; residual = residualScore override else inherent x (1-effectiveness) with effectiveness from controlEffectiveness/100 else controls-count map 0->0/1->20%/2->35%/3->50%/>=4->60%; delta max(0, inherent-residual); reductionPct 1dp; direction reduced/increased/unchanged; rows residual desc then id asc; aboveAppetiteCount STRICT > default 12; avgReductionPct 1dp; worst capped 5; malformed -> EMPTY_RESIDUAL_TRACKING), `generateTreatmentPlans` (only open/mitigating/undefined-status risks >= threshold default 10; strategy ladder >=16 mitigate / 10-15 transfer on /transfer|insurance|contract/i control keyword else mitigate; priorities P0>=16/P1>=10/P2>=5/P3 counted for ALL assessable risks; P0 prepends escalation action capped 5; P0/P1 deadlines = injected clock + horizonDays default 30, others null; owner passthrough or 'unassigned'; plans priority asc then score desc then id asc; byStrategy planned-only; nextDeadline earliest non-null; malformed -> EMPTY_TREATMENT_PLAN_RESULT). New tRPC factory `server/routers/riskQuantification.ts` (createRiskQuantificationRouter(t, protectedProcedure): protected pure appetite/matrix/residual/treatmentPlans queries, exported zod schemas incl. appetite input with nested config block + shared clockOptionSchema, loose scale fields engine-coerced at the boundary, zod BAD_REQUEST only, no DB access) wired as `riskQuantification:` in routers.ts. UI: pages/riskQuantificationApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_* shapes, retry:false hooks useRiskAppetite/useRiskMatrix/useResidualRisk/useTreatmentPlans, QUANT_BAND/APPETITE_VERDICT/RESIDUAL_DIRECTION/TREATMENT_PRIORITY/TREATMENT_STRATEGY meta helpers + quantBandForScore/residualBarClass, DEMO_QUANT_RISKS + buildDemo* per sec.17 with fixed demo clock) + pages/cyber/RiskQuantificationPanels.tsx "NIS2 Risk Quantification" section (appetite strip with utilization bar + verdict tiles + breach list + recommendations; 5x5 heat matrix with token-based cell fills + band counters + top risks; residual table with reduction bars + direction badges + above-appetite highlight; treatment plan cards with priority/strategy badges + action checklists + deadline countdowns; demo-mode amber banner behind an off-by-default Switch, Skeleton/EmptyState degradation; token-only, dark-mode safe) + additive embeds on the RiskRegister overview tab (fragment-wrapped) and RiskHeatmapPage. QA: +54 tests (31 engine incl. strict-> verdict boundaries at appetite and appetite*(1+tol), invalid-scale exclusion, utilization clamp, breach sort/cap, loss-appetite rec, band edges 4/5 9/10 15/16, residual derivation maps + overrides + increased edge, treatment threshold/strategy-keyword/status eligibility/P0 escalation/deadline clock injection ISO-epoch-Date-factory-throwing, determinism/never-throws/EMPTY-freeze/garbage-row counting; 11 router contract incl. route shape/no-mutations, protected UNAUTHORIZED, zod BAD_REQUEST, loose-boundary coercion witness, clock options, exported schemas, no-DB + no-Math.random purity scan) - suite 1837 -> 1891 green (76 -> 78 files); tsc 2033 baseline unchanged (0 errors in touched files); smoke green; committed to dev (96285ae). All three async agents (BACKEND/UI/QA) died to upstream timeouts with partial work - conductor rebuilt backend + tests + UI from the shared spec, reconciled the UI agent's coherent RiskHeatmapPage embed, and fixed 3 self-inflicted defects mid-cycle (standing recommendation on zero assessed data vs EMPTY contract; breach-count arithmetic slip 15 > 13.2; missing JSX fragment wrapper). Pushed to origin/dev + github/dev.
- **Cycle 25** (4660391): NIS2 threat landscape integration (NIS2 plan Phase 1 Task 1.1, ENISA Measure 1.1 / Art. 21(2)(d)) - new pure deterministic `lib/nis2/threatLandscape.ts` engine (zero deps, never throws, injectable clock): `classifyThreatEvent` (ENISA taxonomy keyword classification over title+description with title-wins precedence, case-insensitive matchedKeywords deduped preserving taxonomy map order TH-001..TH-008, fallback categoryId TH-000/Other impactLevel Medium confidence 0, confidence = min(1, 0.2 x distinct keyword hits) rounded 1dp, nis2Articles from the base threat or []), `generateThreatScenarios` (built-in sector catalog SC-FIN-01 / SC-HC-01 / SC-MFG-01 + general Any scenarios; sector matching exact-token OR substring either direction, case-insensitive, non-string sector treated as absent; sector scenarios PLUS Any scenarios on match, Any-only fallback otherwise; positive limit floored and applied LAST, non-positive/NaN/Infinite ignored; deterministic catalog order; garbage fields never throw), `buildTaraTemplate` (asset x scenario cross-product rows; inherentLikelihood High 3/Medium 2/Low 1 else 1, inherentImpact Critical 4/High 3/Medium 2/Low 1 else 1, riskScore = likelihood x impact with bands critical >=9 / high >=6 / medium >=3 / low; rows sorted riskScore desc then assetName asc then scenarioId asc; perBand counts, avgRiskScore 1dp, topRisks capped 5 same order; mitigations from recommendedControls falling back to mitigations else []; scenarioId ?? id with safe fallbacks), `summarizeThreatLandscape` (totalEvents counts garbage rows; category rollups via classifyThreatEvent sorted categoryId asc; severity counts always 5 buckets unknown-coerced; recentEvents top 5 sorted occurredAt desc then id asc with numeric ids before strings on ties, invalid/missing occurredAt rows LAST by id asc emitted occurredAt null (valid rows ISO strings); 30-day trend last30d/prior30d/delta vs injectable clock excluding future-dated+invalid; exposureScore = round1(100 x sum(weights)/(n x 5)) weights critical 5/high 4/medium 3/low 2/unknown 1, 0 when empty, clamped [0,100]; up to 5 fixed deterministic recommendations in severity precedence omitting zero buckets; optional sector passthrough does not change output; malformed -> EMPTY_THREAT_LANDSCAPE_SUMMARY). New tRPC factory `server/routers/threatLandscape.ts` (createThreatLandscapeRouter(t, protectedProcedure): protected pure classify/scenarios/tara/summary queries, exported zod schemas incl. six input/item schemas with STRICT enums rejecting loose likelihood/criticality strings at the boundary (engine coerces internally), zod BAD_REQUEST only, no DB access) wired as `threatLandscape:` in routers.ts. UI: pages/threatLandscapeApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_THREAT_CLASSIFICATION/_SCENARIOS/_TARA_TEMPLATE/_THREAT_SUMMARY, retry:false hooks, SEVERITY/RISK_BAND/TREND_DELTA meta helpers, DEMO_* + buildDemo* per sec.17) + pages/cyber/ThreatLandscapePanels.tsx "NIS2 Threat Landscape" section on CyberDashboard (classification card, sector scenario cards, TARA table, summary strip with exposure score; demo-mode amber banner behind an off-by-default Switch, Skeleton/EmptyState degradation; token-only, dark-mode safe - UI audit passed with ZERO edits required) + CyberDashboard.tsx additive embed (+4 lines). QA: +43 net tests (113 total across the two new files: 92 engine incl. determinism/never-throws/EMPTY-freeze/garbage-row coverage added by the QA agent + 21 router contract incl. protected UNAUTHORIZED, zod BAD_REQUEST strict-enum rejections, exported schemas, no-DB scan) - suite 1794p/38f -> 1837/1837 green (76 files); tsc 2033 baseline unchanged (0 errors in touched files); smoke green; pushed to origin/dev + github/dev. Conductor completed the interrupted parallel WIP (3 async agents: backend reconciled 38 -> 2 failures before upstream timeout; UI audit clean; QA added 5 tests then errored): fixed 2 test-fixture defects (TARA sorting fixture gave assets a and c identical "High" criticality while expecting divergent 12/9 scores - asset a is Critical per its own inline comments; recentEvents fixture relied on the ev() helper defaulting occurredAt so ev({id:4}) carried a valid timestamp instead of exercising the missing-field path - explicit occurredAt: undefined); deleted stray _inspect.ps1 scratch.
- **Cycle 24** (3d49dbf): NIS2 Evidence Repository (NIS2 plan Phase 6 Task 6.2, ENISA Measures 1.1-12.1 / Art. 21(2)) - new pure deterministic `lib/nis2/evidenceRepository.ts` engine (zero deps, never throws, injectable clock): `suggestEvidence` (13-measure ENISA catalog 1.1..12.1 -> Art 21(2) categories a-j with evidenceTypes/freshnessDays/baseScore per entry; measureId exact-match + category substring + requirementText keyword scoring, limit applied last, sorted baseScore desc then measureId asc, deterministic), `buildEvidenceAuditTrail` (events sorted most-recent-first with null updatedAt last + id asc tie-break; evidenceId falls back to String(id); status coerced to the 6-value enum; daysSinceUpdate floor whole days; summary totals/byStatus/withOwner/withFiles), `analyzeEvidenceQuality` (freshness expired < now / expiring <= 90d / stale never-verified-or-past-interval / fresh; cadence n-a / overdue strict > interval / due-soon >= 75% interval (whole-day floors) / on-track; composite qualityScore = STATUS_BASE 50/35/20/15/0 + FRESHNESS_BONUS 30/15/0 + CADENCE_BONUS 30/15/5/0 + 5 when files, clamped 0-100; band strong>=80 / adequate>=55 / weak; daysUntilExpiry; owner passthrough; nextAction ladder; overall avgQualityScore 1dp + coverageRate 1dp + 8-key counts + capped recommendations). New tRPC factory `server/routers/evidenceRepository.ts` (createEvidenceRepositoryRouter(t, protectedProcedure): protected pure suggest/auditTrail/analyze queries, exported zod schemas incl. evidenceRepositorySuggestInputSchema/AuditTrailInputSchema/AnalyzeInputSchema + row schemas + shared clockOptionSchema, zod BAD_REQUEST only, no DB access) wired as `evidenceRepository:` in routers.ts. UI: pages/evidenceRepositoryApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_EVIDENCE_SUGGESTIONS/AUDIT_TRAIL/ANALYSIS, retry:false hooks, STATUS/FRESHNESS/CADENCE/BAND meta helpers, DEMO_EVIDENCE_ROWS + buildDemo* per sec.17) + pages/EvidenceRepositoryPanels.tsx "NIS2 Evidence Repository" section on the Evidence page (health strip with avg quality + coverage + recommendation chips, quality table with ScoreBar + freshness/cadence badges + owner column + next-action, audit-trail feed with status chips + owner + file counts, suggestion cards keyed to ENISA measures; demo-mode amber banner, Skeleton/EmptyState degradation; token-only, dark-mode safe) + Evidence.tsx additive embed. QA: +98 tests (84 engine incl. catalog integrity 13 measures ascending + frozen + no dupes + article coverage a-j, filter/limit semantics, score math + clamp + determinism, audit-trail coercion/sort/tie-break/summary, quality boundaries expired/expiring 90/91d, stale strict > interval, due-soon 75% floor, band edges 80/75/55/50, next-action ladder, counts + avg + coverage math, recommendations order/cap/omit-zero, clock injection ISO/epoch/Date/factory/throwing-factory + never-throws; 14 router contract incl. route shape/no-mutations, protected UNAUTHORIZED, zod BAD_REQUEST type mismatches, exported schemas, no-DB + no-Math.random purity source scan; 2 uiTokenPurity gate entries) - suite 1626 -> 1724 (72 -> 74 files), all green; tsc 2033 (0 new in touched files; pre-existing dashboard/evidence/federal/routers backlog unchanged); smoke green; pushed to origin/dev + github/dev. Conductor completed the interrupted cycle-24 WIP: fixed 3 test defects (purity source scan now strips comment blocks so rule descriptions don't trip the check; counts.verified expectation corrected to 3 = rows with status "verified" incl. the expired-but-verified row, consistent with the recommendations test; determinism test defines its own fixtures instead of referencing out-of-scope ones) + added owner passthrough to analysis rows (engine interface + mapping, contract layer type, demo builder) resolving the 2 new tsc errors; removed stray `$null` artifact from the tree.
- **Cycle 23** (501e14e): NIS2 Policy Center (NIS2 plan Phase 6 Task 6.1, ENISA Measures 1.1-12.1 / Art. 21(2)) - new pure deterministic `lib/nis2/policyTemplates.ts` engine (zero deps, never throws, injectable clock): `getNis2PolicyTemplates` (13-template built-in catalog, one per ENISA measure row of the plan mapping table -> NIS2 Art. 21(2) category a-j -> ISO/IEC 27001:2022 controls; exact category/measureId filters, case-insensitive search over title/summary/enisaMeasureTitle, positive-int limit applied last; items sorted enisaMeasureId NUMERICALLY asc (1.1, 2.1, .., 9.1, 10.1, 11.1, 12.1) then title asc; malformed -> EMPTY_POLICY_TEMPLATE_LIST), `analyzePolicyGap` (covered = any implemented policy sharing an ISO control OR a >=3-char title word case-insensitively; every row counts regardless of status; coverageRate 1dp 0 on zero denominator; gaps with uncovered controls + deterministic recommendations; numeric ENISA sort then title; byIsoControl rollups templateCount + covered/gap status sorted asc; recommendations capped 5; malformed -> EMPTY_POLICY_GAP_ANALYSIS), `runPolicyApproval` (status normalized draft/in_review/approved/rejected/changes_requested/unknown; verdict precedence Approved > Rejected > Changes requested from decisions, else status-derived Draft / Awaiting review / Approved / Rejected / Changes requested / Unknown - lowercase "unknown" only when no status supplied; approvalCount/rejectionCount/changesRequestedCount/pendingCount; reviewProgress = decisions/requiredApprovals*100 1dp, default requiredApprovals 1 (absent valid, explicit invalid -> 0); complete = approvalCount >= requiredApprovals; overdue strict at submittedAt + slaDays*86400000 default 14d; daysInReview floor; 4-step workflow done/current/pending; reviewers enriched + sorted decidedAt asc nulls last then id asc; malformed -> EMPTY_POLICY_APPROVAL), `trackPolicyVersions` (versions sorted createdAt asc nulls last then label asc; last sorted entry 'current' unconditionally, others 'superseded' unless draft-normalized; latestVersion/currentVersion = last entry with a real label - garbage rows never current; createdAt ISO or null; counts; changes = non-empty changeSummaries in order; EMPTY_POLICY_VERSION_HISTORY zeroed + frozen, policyId undefined). New tRPC factory `server/routers/policyTemplatesNis2.ts` (createPolicyTemplatesNis2Router(t, protectedProcedure): protected pure templates / gapAnalysis / approvalWorkflow / versionHistory queries, exported zod schemas incl. policyTemplatesNis2TemplatesInputSchema / GapAnalysisInputSchema / ApprovalInputSchema / VersionsInputSchema + item schemas, shared clockOptionSchema now(string|number)/clock(fn), zod BAD_REQUEST only, no DB; approvalWorkflow exposes the contract-named `currentStatus` beside the engine's `status`) wired as `policyTemplatesNis2:` in routers.ts. UI: pages/policyTemplatesNis2Api.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false hooks, NIS2_* types mirroring the backend contract incl. currentStatus + verdict casing, GAP/APPROVAL/VERSION meta helpers, DEMO_* + buildDemo* per sec.17) + pages/cyber/PolicyCenterPanels.tsx "NIS2 Policy Center" section on CyberDocumentation (template catalog table with ENISA measure/Art 21 category/ISO controls/review cadence; gap analysis with coverage rate + uncovered templates + ISO-control rollups + top recommendations; approval workflow with currentStatus badge + verdict + reviewProgress bar + 4-step pipeline + reviewer decisions; version history with latest/current/superseded/draft statuses + change log; demo-mode amber banner, Skeleton/EmptyState degradation; token-only, dark-mode safe) + CyberDocumentation.tsx additive embed. QA: +127 tests (99 engine incl. numeric ENISA ordering, filters/search/limit, coverage matching, gap sort/tie-break/recommendation cap, verdict precedence + casing, count tallies, reviewProgress 1dp + default/invalid-zero + complete boundary, overdue strict boundary + sla override, daysInReview floor, step statuses, reviewer enrichment/sort/garbage, version sort/ties/latest-current/draft/counts/label fallback/garbage, EMPTY freezing + determinism + clock injection; 26 router contract incl. protected UNAUTHORIZED, zod BAD_REQUEST, exported schemas, no-DB, currentStatus witness; 2 uiTokenPurity gate entries) - suite 1499 -> 1626 (72 files), all green; tsc 2033 (0 new in touched files; pre-existing dashboard/evidence/federal/routers backlog unchanged); smoke green; pushed to origin/dev + github/dev. Conductor reconciled the interrupted cycle-23 parallel WIP to the test contract (numeric ENISA sort comparator; title-cased verdicts with lowercase "unknown" for missing status only; added rejectionCount/changesRequestedCount/pendingCount; reviewProgress/complete against default requiredApprovals=1; latestVersion skips empty garbage labels; EMPTY_POLICY_VERSION_HISTORY.policyId undefined; router approvalWorkflow adds currentStatus). internalLinks 30s timeout in the parallel run confirmed as the known OneDrive flake (passes standalone in <1.5s).
- **Cycle 22** (72224cd): NIS2 continuous compliance monitoring (NIS2 plan Phase 5 Task 5.2, ENISA Measure 7.1 "Effectiveness Assessment" / Art. 21(2)(f)) - new pure deterministic `lib/nis2/complianceMonitor.ts` engine (zero deps, never throws, injectable clock): `computeCompliancePosture` (equal-weight average of valid finite scores clamped 0-100 1dp; bands on the UNROUNDED average Strong>=85 / Developing>=65 / At Risk>=40 / Critical<40, empty -> "No Data" with score 0; coverageRate = valid/total 1dp, 0 when total 0; driftPts = mean of per-measure (score - baselineScore) 1dp, null when no baseline present; trend improved >= +0.05 / declined <= -0.05 / stable, n-a when driftPts null; statusCounts strong/developing/atRisk/critical/noData; topGaps numeric-score measures only, score asc then measureId asc, sliced 5; totalMeasures/assessedMeasures + deterministic verdict; malformed -> EMPTY_COMPLIANCE_POSTURE), `trackEvidenceCoverage` (covered = finite evidenceCount > 0; expiry evaluated ONLY for covered items with valid expiresAt: expired when < now, expiring when 0 <= delta <= 90 days (exactly 90d AND exactly 0 expiring), current otherwise, invalid/missing -> n-a; coverageRate = covered/total 1dp; avgEvidencePerControl 1dp; byMeasure rollup coverageRate asc then measureId asc with empty-key bucket; items sorted controlId asc, null/undefined last; malformed -> EMPTY_EVIDENCE_COVERAGE), `generateAuditReport` (postureScore derived from measures when absent ((90+80)/2=85) else explicit value; coverageRate from evidenceSummary total/covered or explicit; topGaps = assessed measures below the Strong bar (< 85), score asc then measureId asc capped 5; section statuses: posture pass >= 85 / warn >= 65 / fail, evidence pass >= 80 / warn >= 50 / fail, gaps fail when any top gap else pass, measures always informational; deterministic plain-text markdown report with entity name/sector/generatedAt (injected clock)/measure-score rows/top-gap block/recommendation block; recommendationCount = topGaps.length; malformed non-object -> EMPTY_AUDIT_REPORT with report ""). Router rewritten from the old DB-backed implementation to a pure facade `server/routers/complianceMonitor.ts` (createComplianceMonitorRouter(t, protectedProcedure): protected pure posture / evidenceCoverage / auditReport queries, exported zod schemas incl. complianceMonitorPostureInputSchema / EvidenceCoverageInputSchema / AuditReportInputSchema + measure/evidence-item/evidence-summary item schemas, shared clockOptionSchema now(string|number)/clock(fn), zod BAD_REQUEST only, no DB access) wired as `complianceMonitor:` in routers.ts (moved to protectedProcedure next to the other NIS2 engines; old premiumClient/admin wiring removed). UI: pages/complianceMonitorApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_COMPLIANCE_POSTURE/EMPTY_EVIDENCE_COVERAGE/EMPTY_AUDIT_REPORT, retry:false hooks useCompliancePosture/useEvidenceCoverage/useAuditReport, POSTURE_STATUS_META/TREND_META/EVIDENCE_STATUS_META/EXPIRY_META/AUDIT_SECTION_STATUS_META + formatScore/coverageBarClass helpers, DEMO_COMPLIANCE_MEASURES/DEMO_EVIDENCE_ITEMS + buildDemo* per sec.17 with fixed demo clock) + pages/cyber/ComplianceMonitorPanels.tsx "Continuous Compliance Monitoring" section on CyberDashboard (posture scorecard with gauge + status counts + top-gap list, evidence-coverage panel with per-measure rollups + expiry badges + expiring/expired strips, audit report preview with pass/warn/fail section chips + markdown body; demo-mode amber banner, Skeleton 1:1 + EmptyState "Connect the complianceMonitor.<proc> API" degradation; token-only, dark-mode safe) + CyberDashboard.tsx additive embed. Component migrations off the removed DB-backed procedures: ComplianceHealthWidget + ContinuousComplianceScore now drive the pure `complianceMonitor.posture` via the contract layer with deterministic demo input (removes the old Math.random()-based score); MSSPRevenueWidget no longer calls the removed getMonitorSummary/getAllClientHealths - revenue computed from trpc.clients.list. QA: +94 tests (74 engine incl. posture band boundaries 85/84.9, 65/64.9, 40/39.9 on unrounded averages, drift thresholds +0.05/0.0499/-0.0499, topGaps sort/tie-break/slice-5, coverage math, expiry boundaries 1ms-before/at-0/90d/90d+1ms + n-a rules, byMeasure rollup/sort, audit report section statuses + derivation + determinism + clock injection + garbage rows, EMPTY shape freezing; 18 router contract incl. protected UNAUTHORIZED, zod BAD_REQUEST, exported schemas, no-DB source scan; 2 uiTokenPurity gate entries) - suite 1405 -> 1499 (70 files), all green; tsc 2033 (-8 vs 2041 baseline; 0 new in touched files after the MSSPRevenueWidget as-any cast fix 3c2ca89; pre-existing dashboard/evidence/federal/routers backlog unchanged); smoke green. Conductor reconciled 8 test failures: round1 float-error epsilon + -0 normalization (drift +0.05 boundary), audit topGaps = below-Strong bar (< 85) semantics + recommendationCount, clockOptionSchema split into spreadable clockOptionFields + parseable zod schema; migrated 3 legacy widgets off the removed DB-backed procedures (ComplianceHealthWidget + ContinuousComplianceScore -> pure posture via contract layer; MSSPRevenueWidget -> trpc.clients.list via the established as-any cast, fix 3c2ca89).
- **Cycle 21** (e3ac441): NIS2 advanced security testing (NIS2 plan Phase 5 Task 5.1, ENISA Measure 6.7 "Network Security" / Art. 21(2)(e)) - new pure deterministic `lib/nis2/securityTesting.ts` engine (zero deps, never throws, injectable clock): `planPenetrationTest` (test-type/status lifecycle draft->scheduled->in_progress->completed|reported; overdue = scheduled/in_progress AND scheduledDate + windowHours < now (strict, default 336h, per-test override); risk-tier frequency critical 91d quarterly / high 182d semi-annual / medium 365d annual / low+info 730d biennial, unknown->annual; nextDueDate = lastCompletedAt + cadence, fallback scheduledDate; countsByStatus/countsByType kebab->camel keys; overdueTests scheduledDate asc then id asc; malformed -> EMPTY_PEN_TEST_PLAN), `runRedTeamExercise` (7 MITRE ATT&CK phases recon..reporting with tacticCodes; phase pending|active|completed; isOverdue active AND due<now (due = exercise endAt else estimatedEndAt); overall completed>paused>active>not-started; verdict only when completed; roles operator|observer|decision-maker, invalid->observer, dedup last-wins; summary counts), `assessSecurityBenchmarks` (CIS IG1-3 + NIST CSF 2.0 govern..recover; status pass|fail|not-applicable|na|n-a, unknown skipped; categoryScore = pass/(pass+fail)*100 1dp; overallScore = passRate; topGaps capped 5 with keyword remediation), `trackScanCoverage` (5 asset classes; due = dueAt override else lastScanAt+freq; inSla due>=now; byClass rollup array; daysOverdue floor signed). New tRPC factory `server/routers/securityTestingNis2.ts` (createSecurityTestingNis2Router(t, protectedProcedure): protected pure planTest/redTeam/benchmarks/scanCoverage queries, exported zod schemas, zod BAD_REQUEST only, no DB) wired as `securityTestingNis2:` in routers.ts. UI: pages/securityTestingNis2Api.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false hooks, meta helpers, DEMO_* + buildDemo* per sec.17) + pages/cyber/SecurityTestingPanels.tsx "NIS2 Advanced Security Testing" section on SecurityTesting (pen-test scheduler table, red team kill-chain tracker, benchmark scorecards + top gaps, scan SLA coverage + overdue list; demo-mode amber banner, Skeleton/EmptyState degradation; token-only, dark-mode safe) + SecurityTesting.tsx token-only polish + embed. QA: +95 tests (73 engine incl. overdue strict boundaries, frequency math, clock injection, red-team status precedence/participant dedup, benchmark score/gap sort/cap, scan SLA/dueAt precedence; 21 router contract incl. protected UNAUTHORIZED, zod BAD_REQUEST, exported schemas, no-DB scan; 1 uiTokenPurity gate extended) - suite 1310 -> 1405 (68 files), all green; tsc 2041 (0 new in touched files; pre-existing SecurityTesting.tsx + routers.ts backlog baseline unchanged); smoke green; pushed to origin/dev + github/dev. Conductor reconciled 3-way contract drift (engine vs router tests vs UI layer) to the engine contract: byStatus/byType -> countsByStatus/countsByType camel, exercise wrapper -> top-level red-team input, dueAt -> estimatedEndAt, class -> assetClass, result/title -> status/controlName, planned -> pending, IG1/Govern -> ig1/govern, byClass map -> rollup array.
- **Cycle 20** (42548dd): NIS2 security metrics & reporting (NIS2 plan Phase 4 Task 4.2, ENISA Measure 7.1 "Effectiveness Assessment" / Art. 21(2)(f)) - new pure deterministic `lib/nis2/securityMetrics.ts` engine (zero deps, never throws, injectable clock): `computeMttr` (only rows with valid detectedAt + resolvedAt and resolvedAt>=detectedAt count; per-severity buckets critical..unknown in deterministic order + overall rollup {mttrHours/minHours/maxHours 1 decimal, mttrDays 2 decimals, count}; totalResolved; malformed -> EMPTY_MTTR_RESULT, never throws), `trackVulnerabilityAge` (whole-day ages vs injected clock; terminal statuses patched/risk-accepted/false-positive excluded case-insensitively; overdue thresholds critical 30d/high 60d/medium 90d/low 120d/info 180d/unknown 180d, STRICTLY > threshold; overall applies each row's own severity threshold; age-band counts 0-7/8-30/31-60/61-90/90+; avgAgeDays 1 decimal + maxAgeDays; deterministic id-asc tie-break; malformed -> EMPTY_VULNERABILITY_AGE_RESULT), `detectComplianceDrift` (scores clamped 0-100, driftPts = current-baseline 1 decimal; alert bands <= -20 critical / <= -10 high / <= -5 medium / <= -0.05 low, improved >= 0.05 else stable; catalog recommendations per severity, '' for stable/improved; dedup by areaId last-wins; alerts sorted driftPts asc then areaId asc; counts critical/high/medium/low/stable/improved + totalAlerts + totalAreas; malformed -> EMPTY_COMPLIANCE_DRIFT_RESULT), `buildExecutiveSummary` (postureScore clamped 0-100 1 decimal; counts whole non-negative; MTTR trend vs prior: pctChange = (prior-current)/prior*100 1 decimal, improved/worsened/flat or n-a when prior missing/0; status bands Critical when driftAlertCount>=5 OR overdueVulns>=10 OR postureScore<50, Watch when >=1/>=1/<75, else Good; topAreasAtRisk dedup + driftPts asc + areaId asc sliced to 3; non-object -> EMPTY_EXECUTIVE_SUMMARY). New tRPC factory `server/routers/securityMetrics.ts` (createSecurityMetricsRouter(t, protectedProcedure): protected pure mttr/vulnerabilityAge/complianceDrift/executiveSummary queries, exported zod schemas incl. securityMetricsMttrInputSchema/VulnerabilityAgeInputSchema/ComplianceDriftInputSchema/ExecutiveSummaryInputSchema + item schemas, shared clockOptionSchema now(string|number)/clock(fn), zod BAD_REQUEST only, no DB access) wired as `securityMetrics:` in routers.ts. UI: pages/securityMetricsApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_MTTR/EMPTY_VULNERABILITY_AGE/EMPTY_COMPLIANCE_DRIFT/EMPTY_EXECUTIVE_SUMMARY, retry:false hooks useMttr/useVulnerabilityAge/useComplianceDrift/useExecutiveSummary, MTTR_SEVERITY_META/DRIFT_SEVERITY_META/AGE_BAND_META/TREND_META/POSTURE_STATUS_META + trendGlyph(inverted)/formatMetricHours helpers, DEMO_MTTR_INCIDENTS/DEMO_VULNERABILITY_ITEMS/DEMO_DRIFT_AREAS + buildDemo* per sec.17 with fixed demo clock) + pages/cyber/SecurityMetricsPanels.tsx "NIS2 Security Metrics & Reporting" section on CyberMonitoring (executive summary strip with posture score + status badge + trend arrows, MTTR per-severity table with hours/min/max + overall, vulnerability age table with avg/max/overdue + age-band chips, compliance drift alert cards with severity badges + recommendations + stable/improved counters; demo-mode amber banner, Skeleton/EmptyState degradation; token-only, dark-mode safe) + CyberMonitoring.tsx additive embed. QA: +64 tests (50 engine incl. MTTR aggregation math + 1/2-decimal rounding + zero-duration rows + bucket order + malformed/never-throws/determinism, age clock injection ISO/epoch/Date/factory/throwing-factory, floor age math, band boundaries 0-7/8-30/31-60/61-90/90+, overdue strict-boundary 30/31 60/61 90/91 120/121 180/181 per severity + overall per-row thresholds, terminal exclusion case-insensitive, avg/max math, drift band boundaries -20/-19.9 -10/-9.5 -5/-4.5 + stable/improved edges, score clamping, dedup last-wins + sort driftPts asc then areaId asc, exec-summary score/count coercion, MTTR trend improved/worsened/flat/n-a + pct math, status band boundaries 5/4, 10/9, 50/49.9, 75/74.9, top-areas dedup/sort/slice-3 + rounding; 14 router contract incl. route shape/no-mutations, protected UNAUTHORIZED, zod BAD_REQUEST type mismatches (Date-object timestamps rejected, loose enum strings engine-coerced), clock options string/number/fn accepted + Date/string-fn rejected, exported schemas, no-DB source scan) - suite 1246 -> 1310 (66 files), all green; tsc 2041 (0 new in touched files; pre-existing trpc.threatIntel.* + routers.ts backlog baseline unchanged); smoke green; pushed to origin/dev + github/dev.
- **Cycle 19** (f7b2d18): NIS2 CVE & vulnerability tracking (NIS2 plan Phase 4 Task 4.1, ENISA Measure 7.1 / Art. 21(2)(d)) - new pure deterministic `lib/nis2/vulnerabilityMgmt.ts` engine (zero deps, never throws, injectable clock): `classifyVulnerability` (CVSS severity bands >=9 critical / >=7 high / >=4 medium / >0 low / else info, cvssBand range label + 'unknown' on null/NaN, cveId normalized to uppercase CVE-YYYY-NNNN+ else null, isKev/assetCriticality coerced (invalid -> unknown), stable deduped reason list capped 5), `prioritizePatch` (0-100 clamped score = severityBase critical40/high30/medium15/low5/info0 + KEV25 + assetCriticalityBase critical15/high10/medium5/low0/unknown0 + exploitabilityBase active20/weaponized15/proof-of-concept8/none0/unknown0; KEV always P0, else score>=80 P0 / >=60 P1 / >=40 P2 / else P3; remediationSlaDays P0 7 (KEV 1) / P1 30 / P2 90 / P3 180, slaDays override honored; targetDate from injected clock; rationale capped 5), `assessZeroDay` (isZeroDay = KEV || (no cveId && high/critical); verdict immediate-action 24h / escalate 72h / monitor 168h / informational (null sla+deadline); verdict-specific requiredActions catalog capped 5), `getVulnerabilityRegisterSummary` (status/severity counts (kebab->camel keys), overdue = dueAt<now, due-soon = 0<daysUntilDue<=7, terminal patched/risk-accepted/false-positive -> slaStatus 'n-a' + excluded from overdue/due-soon, patchComplianceRate = patched/(open+inProgress+patchScheduled+patched+riskAccepted) 1 decimal / 0 on zero denominator, per-row daysOpen/daysUntilDue/slaStatus enrichment, deterministic sort severity desc -> dueAt asc (nulls last) -> cveId asc -> id asc; malformed -> zeroed, never throws). New tRPC factory `server/routers/vulnerabilityMgmt.ts` (createVulnerabilityMgmtRouter(t, protectedProcedure): protected pure classify/prioritize/zeroDay/registerSummary queries, exported zod schemas incl. vulnerabilityClassifyInputSchema/vulnerabilityPrioritizeInputSchema/vulnerabilityZeroDayInputSchema/vulnerabilityRegisterSummaryInputSchema, zod BAD_REQUEST only, no DB access) wired as `vulnerabilityMgmt:` in routers.ts. UI: pages/vulnerabilityMgmtApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_VULNERABILITY_REGISTER_SUMMARY/EMPTY_ZERO_DAY_ASSESSMENT, retry:false hooks, SEVERITY_META/PRIORITY_META/VULN_STATUS_META/SLA_STATUS_META/ZERO_DAY_VERDICT_META helpers, DEMO_VULNERABILITY_REGISTER_ITEMS incl. Log4Shell/EternalBlue/MOVEit/PAN-OS KEVs + buildDemo* per sec.17) + pages/cyber/VulnerabilityRegisterPanels.tsx replacing the empty "Vulnerability Register" tab on VulnerabilityManagement (4 stat cards open/overdue/patch-compliance/KEV, zero-day response panel with verdict accent + SLA countdown + required-actions checklist, density register table with CVE+KEV badge/severity/priority/status/due countdown/days-open, demo-mode amber banner, Skeleton/EmptyState degradation; token-only, dark-mode safe) + VulnerabilityManagement.tsx additive embed (CVE Intelligence tab untouched). QA: +98 tests (78 engine incl. band boundaries 9/8.9, 7/6.9, 4/3.9, 0.1/0/null, score weight math + clamp/floor, KEV->P0, slaDays override + clock, zero-day verdicts + 24/72/168h windows, register aggregation + SLA windows + terminal exclusion + compliance math + sorting + malformed/determinism/never-throws; 20 router contract incl. protected UNAUTHORIZED, zod BAD_REQUEST, exported schemas, no-DB scan) - suite 1148 -> 1246 (64 files), all green; tsc 2041 (0 new in touched files; pre-existing trpc.threatIntel.* baseline untouched); smoke green; pushed to dev.
- **Cycle 18** (35dfa96): NIS2 third-party risk scoring (NIS2 plan Phase 3 Task 3.2, ENISA Measure 5.1 / Art. 21(2)(d)) - new pure deterministic `lib/nis2/thirdPartyRisk.ts` engine (zero deps, never throws, injectable clock): `aggregateSupplierRisk` (composite 0-100 = 0.4 x criticalityNumeric (classifySupplierCriticality band map critical=90/high=70/medium=50/low=30/unknown=0) + 0.4 x posture score (scoreSupplierSecurityPosture, No Data -> 0) + 0.2 x residual (explicit vendorResidualScore or computeVendorRiskTier derivation from vendor signals; redistributed 50/50 when absent); risk tier from the UNROUNDED weighted sum with bands critical>=75/high>=55/medium>=30/low<30 (conductor fix for the QA-reported rounding bug: 74.99 stays high, 54.99 medium, 29.99 low); verdict + deduped recommendedActions capped 5; totals count/per-tier/avgCompositeScore 1 decimal; malformed -> zeroed, never throws), `trackSupplierCertificate` (CERTIFICATE_TYPES catalog ISO 27001 / SOC 2 Type I-II / PCI DSS v4 / HIPAA / GDPR / Cyber Essentials / NIST CSF; statuses valid / expiring (validTo-now <= 90d) / expired vs clock; missing rows for cert-less suppliers; signed daysUntilExpiry; summary total/valid/expiring/expired + coverageRate 3 decimals = suppliers with >=1 valid-or-expiring cert / total), `buildSupplyChainMap` (nodes composite desc then name asc; edges default weight by source tier critical 1.0/high 0.8/medium 0.5/low 0.3 with explicit override; summary tierCounts/avgCompositeScore/edgeCount). New tRPC factory `server/routers/thirdPartyRisk.ts` (createThirdPartyRiskRouter(t, protectedProcedure): protected pure aggregateRisk/certificates/supplyChainMap queries, exported zod schemas incl. thirdPartyAggregateRiskSchema/thirdPartyCertificatesSchema/thirdPartySupplyChainMapSchema, zod BAD_REQUEST only, no DB access) wired as `thirdPartyRisk:` in routers.ts. UI: pages/thirdPartyRiskApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY_AGGREGATE_RISK/EMPTY_CERTIFICATES/EMPTY_SUPPLY_CHAIN_MAP, retry:false hooks, RISK_TIER_META/CERT_STATUS_META/VERDICT_META helpers, DEMO_TPR_SUPPLIERS/DEMO_TPR_DEPENDENCIES/DEMO_TPR_CERTIFICATE_SPECS + buildDemoAggregateRisk/Certificates/SupplyChainMap per sec.17) + pages/cyber/ThirdPartyRiskPanels.tsx "Third-Party Risk Scoring" section on the SupplyChainRisk page (AggregatedRiskScorecard 6 totals tiles + per-supplier composite bars/tier badges/verdict/top actions; CertificateTracker table with status badges + expiry countdown + coverage-gap rows; SupplyChainMapSummary tier distribution + weighted dependency highlights; demo-mode amber banner, Skeleton/EmptyState degradation; token-only, dark-mode safe) + SupplyChainRisk.tsx additive embed. QA: +46 tests (31 engine incl. band boundaries on unrounded sums, weight math 40-40-20 + 50-50 redistribution, rounding, malformed/determinism/never-throws, clock injection, cert 90d window + coverage, map ordering + default edge weights; 15 router contract incl. protected rejection, zod BAD_REQUEST, exported schemas, no-DB) - suite 1102 -> 1148 (62 files), all green; tsc 2041 (0 new in touched files); smoke green; pushed to origin/dev + github/dev.
- **Cycle 17** (4e76957): NIS2 supplier security lifecycle (NIS2 plan Phase 3 Task 3.1, ENISA Measure 5.1 / Art. 21(2)(d)) - new pure deterministic `lib/nis2/supplyChain.ts` engine (zero deps, never throws, injectable clock): `classifySupplierCriticality` (additive 0-100 criticality - essential services +45 / sensitive data +30 / broad network +25 / restricted +15 / subcontractor -10 / spend >= EUR 1M +10, clamped; bands critical>=70/high>=50/medium>=25; review cadence Quarterly/Semi-Annual/Annual/Biennial + nextReviewDate; stable rationale), `scoreSupplierSecurityPosture` (8 weighted contractual controls summing to 100 - security requirements 20 / incident notification 15 / vulnerability handling 15 / audit rights 12 / subcontracting 10 / termination-exit 10 / data protection 10 / business continuity 8; readiness Strong>=85/Developing>=65/At Risk; No Data when no answers; gaps + gap-derived recommendedActions), `trackSupplierIncident` (24h significant / 72h else notification SLA, custom slaHours honoured; deadline = detectedAt + SLA; status precedence acknowledged > submitted > overdue > due (12h window) > pending; signed daysRemaining, null when reported/acknowledged; deterministic kebab incidentId slug), `monitorSecuritySla` (cadence-driven not-tested/breached >2x/at-risk >1x/compliant; signed daysUntilDue; invalid items filtered, zeroed summary). New tRPC factory `server/routers/supplyChain.ts` (createSupplyChainRouter(t, protectedProcedure): protected pure classifySupplier/scorePosture/trackIncident/monitorSla queries, exported zod schemas, zod BAD_REQUEST only, no DB access) wired as `supplyChain:` in routers.ts. UI: pages/supplyChainApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false hooks, CRITICALITY/READINESS/INCIDENT/SLA meta helpers, DEMO_SUPPLIER_SLA_ITEMS per sec.17) + pages/cyber/SupplyChainLifecyclePanels.tsx "NIS2 Supplier Security Lifecycle" section on the SupplyChainRisk page (criticality classifier, 8-question posture scorecard, incident notification tracker, demo-mode SLA monitor with amber banner; Skeleton/EmptyState degradation; token-only, dark-mode safe) + SupplyChainRisk.tsx token-only conversion. QA: +79 tests (61 engine incl. band boundaries/factor sums/clamp/cadence dates/readiness bands/status precedence + due window/SLA hours/daysRemaining/slug sanitization/invalid-item filtering/determinism/never-throws; 18 router contract incl. zod BAD_REQUEST + no-DB + exported schemas) - suite 1023 -> 1102 (60 files), all green; tsc 2041 (0 new in touched files; only pre-existing routers.ts backlog shifted by the 2 wiring lines); smoke green; pushed to origin/dev + github/dev.
- **Cycle 16** (8a62697): NIS2 incident timeline & escalation triggers (NIS2 plan Phase 2 Task 2.2, Must-Have #2) - new pure deterministic `lib/cyber/incidentTimeline.ts` engine (zero deps, never throws, injectable clock): `buildIncidentTimeline` (4 ordered phases Detection -> Early warning (24h) -> Incident notification (72h) -> Final report (30d); deadline anchors + 12h due-window statuses reused from incidentClassifier.getReportingDeadlines; completed/current/upcoming lifecycle; isComplete when all sent; malformed input -> EMPTY_INCIDENT_TIMELINE), `computeEscalations` (deterministic ordered triggers: monitor/info below Art. 23(3) significance; sev-escalation/warning for high/critical significant before notification; ew/notif/final-due/warning in the 12h window; ew/notif/final-overdue/critical with dueBy; complete/info when all sent; never throws, no duplicate ids). New tRPC factory `server/routers/incidentTimeline.ts` (createIncidentTimelineRouter(t, protectedProcedure): protected pure timeline/escalations queries sharing exported incidentTimelineInputSchema, zod BAD_REQUEST only, no DB access) wired as `incidentTimeline:` in routers.ts. UI: pages/incidentTimelineApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false, INCIDENT_PHASE_META/ESCALATION_LEVEL_META + helpers) + CyberIncidentDetail "NIS2 Incident Timeline" panel (4-step vertical timeline, status badges, current-phase highlight, color-coded escalation list with due-by + Overdue badges, Skeleton/EmptyState degradation) + CyberIncidentReporting critical-escalation callout; token-only, dark-mode safe. QA: +39 tests (28 engine incl. boundaries/malformed/determinism/clock injection; 11 router contract) - suite 984 -> 1023 (58 files), all green; tsc 2041 (0 new in touched files; pre-existing trpc.cyber.* + routers.ts backlog unchanged); smoke green; pushed to origin/dev + github/dev.
- **Cycle 15** (50fd8e5): NIS2 Art. 23 incident classification & severity scoring (NIS2 plan Phase 2 Task 2.1, Must-Have #1) - new pure deterministic lib/cyber/incidentClassifier.ts engine (zero deps, never throws): classifyIncident (NIS2 Art. 23(3) significance across 7 criteria with stable reason list; additive 0-100 severity score - public safety +40, critical infra +35, data integrity +30, cross-border +20, >1000 users +20, >120min +15, >EUR 10k loss +15, clamped 0-100; bands critical>=80/high>=55/medium>=30; ENISA threat category via ordered cause-keyword map over the threat-intel taxonomy, fallback Other/TH-000; nextDeadline 24h when significant), getReportingDeadlines (24h early warning / 72h incident notification / 30d final report with pending/due/overdue lifecycle vs injectable clock, 12h due window), buildCsirtTemplate (plain-text NIS2 notification addressed to the EU competent authority from lib/nis2/competent-authorities.ts, generic fallback, never throws). New tRPC factory server/routers/incidentClassifier.ts (createIncidentClassifierRouter(t, protectedProcedure): protected pure classify/deadlines/csirtTemplate queries, zod BAD_REQUEST only, no DB access) wired as incidentClassifier: in routers.ts. UI: pages/incidentClassifierApi.ts contract layer (UI-STANDARD sec.16 cast pattern, EMPTY shapes, retry:false) + CyberIncidentDetail NIS2 Classification panel (severity badge, score bar, significance verdict + reasons, ENISA category chip, deadline tracker with status badges, CSIRT template preview with EU country select) + CyberIncidentReporting quick-callout; token-only, dark-mode safe. QA: +92 tests (80 engine incl. threshold boundaries/malformed-input/determinism/clock injection; 12 router contract) - suite 892 -> 984 (56 files), all green; tsc 2041 (0 new in touched files; 3 pre-existing trpc.cyber.* errors unchanged); smoke green; pushed to origin/dev + github/dev.
- **Cycle 14** (7081639): AI-powered compliance productized (P3 #10 done) — new pure deterministic `lib/ai/copilot.ts` engine (zero deps, never throws): `draftPolicyDraft` (10 policy topic templates + generic fallback, framework-specific controls, orgName in title+scope), `suggestEvidenceForControl` (evidence catalog with freshness hints, stable fallback), `autoMapRequirement` (0-100 tiered keyword scoring: verbatim phrase 1.0 > all-token 0.7 > single-token 0.35; ranked matches + bestMatch) over a built-in control KB (SOC 2 / ISO 27001 / NIS2 / GDPR / HIPAA / PCI DSS). Tiered scoring fixes the 'access control' → remote-work template mis-selection. New tRPC factory `server/routers/aiCopilot.ts` (`createAiCopilotRouter(t, protectedProcedure, publicProcedure)`: protected `draftPolicy`/`suggestEvidence`/`autoMap` mutations + public deterministic `status`, zod BAD_REQUEST only throw, no DB access) wired as `aiCopilot:` in routers.ts. UI: pages/aiCopilotApi.ts contract layer (UI-STANDARD §16 cast pattern, retry:false, EMPTY_AI_COPILOT_STATUS) + AgentPage "Compliance copilot" quick-actions (draft/suggest/auto-map cards, token-only, Skeleton + EmptyState degradation). QA: +33 tests (22 engine + 11 router) — suite 859 → 892 (54 files), all green; tsc 0 new errors in touched files (routers.ts 55 = 55 vs HEAD); smoke green. Also committed cycle-13 WIP: AuditManager → auditApi contract layer; trustCenter now registered with publicProcedure (public posture page semantics).
- **Cycle 13** (edec147): Trust center + audit management parity (P2 #8 + #9 done) — trustCenter router hardened (all public procedures graceful-degrade on DB failure, never throw; only business-rule FORBIDDEN kept) + new public `getPosture` procedure (client profile + latest complianceSnapshot score → status Strong/Developing/At Risk/No Data + implemented/total controls + trust documents; neutral empty shape on DB failure/unknown client); auditPackageGenerator hardened (pure helpers computePassRate/buildControlsCsv/buildAuditManifestJson + structured AuditPackageError DB_UNAVAILABLE/CLIENT_NOT_FOUND/GENERATION_FAILED), auditPackage router translates to TRPCError NOT_FOUND/INTERNAL_SERVER_ERROR. UI: token-only conversion of TrustCenter/AuditHub/AuditManager per UI-STANDARD §2 (592 → 0 forbidden slate/gray/white/indigo surface refs; data-viz exceptions kept with dark: variants; invalid /100/ opacity chains + ring/divide-slate fixed); TrustCenter surfaces getPosture (score badge + status + implemented/total) with EmptyState/Skeleton degradation; typed contract layers pages/trustCenterApi.ts + pages/auditApi.ts (UI-STANDARD §16 cast pattern). QA: +34 tests (21 trustCenterRouter contract/degradation, 10 auditPackageGenerator pure helpers, 3 uiTokenPurity static gate) — suite 825 → 859 (52 files), all green; tsc 2043 → 2043 (0 new in touched files); smoke green; pushed to github/dev.
- **Cycle 12** (1f3ac7f): Questionnaires & surveys auto-scoring (P2 #6 done) — deterministic pure `lib/questionnaire/questionnaireScoring.ts` engine (per-answer classification pass/partial/fail/neutral/unanswered, focus-area aggregation with per-area 0-100 scores, completionRate, complianceScore 0-100, readiness Strong/Developing/At Risk/No Data, summarizeScore label/tone/description; zeroed "No Data" on non-array/malformed input, never throws), `lib/questionnaire/templates.ts` built-in sets (SIG Lite ~20 questions across Governance/Access Control/Data Protection/Security Testing/Cryptography/IR/Vendor Mgmt/HR + CCM CAIQ v4 covering all 15 CCM domains AIS/BCR/CCC/CEF/DSI/DCS/GRM/HRS/IVS/LOG/MON/SEF/STA/TVM/UEM; questionCount derived, unique ids, known responseTypes only). Router refactored to `createQuestionnaireRouter(t, clientProcedure, publicProcedure)` factory (test-harness friendly) with new `score` (DB-backed, never throws — DB failure → empty score readiness "No Data", unknown id → null) and `scoreAnswers` (pure passthrough) procedures. UI: questionnaireApi.tsx contract layer (UI-STANDARD §16: useQuestionnaireScore hook, ScoreBadge/ScoreProgress/getReadinessMeta/getScoreToneClass/getScoreBarClass), QuestionnairesDashboard per-row Score column (badge + progress bar, Skeleton while loading, dash on error/empty, colSpan fix), QuestionnaireWorkspace score/readiness surfacing; token-only polish (bg-muted/border-border/bg-card/text-destructive, dark-mode status badges). Conductor fixed 1 new tsc error (getScoreBarClass null-acceptance). Tests 753 → 825 (49 files, +72: 39 scoring + 19 templates + 14 router), all green; coverage 100% on 5 configured targets; tsc 3614 → 3612 (0 new in touched files; known follow-up: neutral answers count toward `answered`/completionRate — asserted as spec).
- **Cycle 11**: TPRM per-vendor risk tiering (P2 #5 done) — pure deterministic `lib/vendor/vendorRisk.ts` engine (inherent risk by data-access class; residual score 0-100 from SOC2 / latest scan / open high-critical assessments / contract / DPA / subprocessor signals; Tier 1-3 + review cadence + next-review date + 3-5 recommended actions; injectable clock; portfolio overview with tier counts / avg score / due-for-review), DB-backed `vendorRisk` tRPC router (getOverview/getVendorRisk over vendors+scans+assessments+contracts+DPAs; never throws on read — empty overview on DB failure; NOT_FOUND for unknown vendor), wired in routers.ts. UI: vendorRiskApi.ts contract layer (UI-STANDARD §16); VendorDashboard risk overview (tier stats, score bars, next-review due list); VendorList per-vendor Risk Tier column (badge + next review, Skeleton/dash degradation); VendorDetails "Vendor Risk" card (score bar, tier, frequency, next review, recommended actions, EmptyState degradation); token-only dark-mode polish across TPRM pages. Backend review: 5/5 schema tables verified, 2 hardening fixes (latest-scan ignores null-riskScore rows; Draft contracts no longer count as active). Tests 666 → 753 (46 files, +87: 24 lib + 9 router + 54 engine), all green; tsc 0 new errors in touched files (0 in all new files).
- **Cycle 9** (4f55b08): Enterprise SSO (P2 #13 gap) — lib/sso/oidc.ts (OIDC discovery, authorization-code URL builder, token exchange, RS256 ID-token verification against JWKS with nonce/issuer/audience/exp checks, claim mapping, single-flight 10-min state store; all injectable-fetch), lib/sso/proxy-auth.ts (case-insensitive reverse-proxy header auth, openId `sso_proxy:<email>`), server/routers/sso.ts (status/start/callback: state consume → code exchange → verify → upsert user by openId loginMethod 'sso' → localAuth session JWT; TRPCError-wrapped, never echoes secrets) wired into routers.ts, authMiddleware proxy-SSO upsert (no auth header path), ENV + .env.example SSO_OIDC_*/SSO_PROXY_* vars. UI: ssoApi.ts contract layer (UI-STANDARD §16), LoginPage "Continue with SSO" button (only when sso.status mode==='oidc', graceful on error), SsoCallback page (/auth/sso/callback → token in localAuthToken/localAuthUser → /dashboard), SecuritySettings read-only SSO status card (token-only, dark-mode safe). QA: 30 oidc + 10 proxy-auth + 3 router tests (43 new; real RS256/JWKS mocking, env restore). Conductor completed wiring (agents hit turn caps) + fixed 2 contract gaps (case-insensitive header lookup; tsc JsonWebKey typing; MutationLike.isSuccess). Tests 623 → 666 (43 files), coverage 100% on 5 targets, tsc 2046 → 2046 (0 new).
- **Cycle 8** (551c9f3): Webhook system (P2 #15 done) — webhookRegistry hardened: bounded retries with exponential backoff (default 2, 4xx never retried, injectable fetch), updateWebhookSubscription / deleteWebhookSubscription / listWebhookEventCatalog, recursive secret scrubbing (payload + delivery log), dispatch never throws (graceful DB degradation, per-write guard). New webhookEvents lib (WEBHOOK_EVENT_CATALOG single source of truth: test.ping, evidence.expired, control.autotest.failed, risk.created, policy.ack.overdue, *; safeDispatchWebhookEvent fire-and-forget via setImmediate). Router: updateSubscription / deleteSubscription / listEventCatalog + event-list validation on subscribe/update, TRPCError-wrapped, bounded listDeliveries limit. Real event wiring (void safeDispatch, never blocking): evidence.expired (renewal loop ×3 paths), control.autotest.failed (auto-test engine fail), policy.ack.overdue (reminder sweep), risk.created (high/critical upsert). UI: Webhooks page (token-only: stat cards, subscriptions CRUD + enable/disable switch + test-ping trigger, deliveries table, create dialog with one-time secret reveal, per-procedure EmptyState degradation; webhooksApi.ts contract layer) + /clients/:id/webhooks route + alias + nav item. Scorecard reconciliation: TPRM / questionnaires / trust center / audit / API rows updated to reflect existing implementations (previously marked "None"). Tests 565 → 623 (40 files), coverage 100% on 5 targets, tsc 2046 → 2046 (0 new; 0 errors in all new files).
- **Cycle 7** (c3b2bf4): Access review automation (P2 #7 done) — access_review_cycles/tasks schema + migration 0022, accessReviews lib (createCycle, idempotent provisionCycle per user x role, listTasks, certify/revoke with notes + reviewer, runOverdueCheck/runGlobalOverdueCheck with injected clock, getSummary, listHistory; DB-backed with in-memory fallback), accessReviews tRPC router (9 client-scoped procedures) wired in routers.ts, accessReviewScheduler (12h overdue sweep, ENABLE_ACCESS_REVIEW_SCHEDULER) + AccessReviews page (UI-STANDARD token-only: stat cards, expandable cycles, certify/revoke workflow, history feed; accessReviewsApi.ts contract layer with graceful EmptyState degradation). GRC cross-module integration + hardening (grc-integration findOrCreateTask/notifyClientOnce; findings->remediation tasks; control auto-test failure digests; evidence expiry->renewal tasks; vendor residual risk->criticality + risk-register entry; DSAR deadline scheduler; unified action center; fabricated-data elimination; CI hardening). Conductor fixed the 7 new tsc errors the feature batch introduced (vendorAssessments duplicate import; msspGovernanceService clientSummaries typing -> MsspPartnerPortal rollup no longer never[]). Tests 524 -> 565 (37 files), coverage 100% on 5 targets, tsc 2045 -> 2041 (0 new; 4 fewer than HEAD), MsspPartnerPortal token-only pass.
- **Cycle 6** (43b3260): Evidence expiration & renewal (P1 #14 done) — evidenceRenewal lib (due-for-renewal horizon, renewal loop with auto-remediation + remediation notes), tRPC router (getSummary/runNow), scheduler wired (ENABLE_EVIDENCE_RENEWAL_SCHEDULER), EvidenceRenewalPanel on Evidence page with graceful fallback to live evidenceExpiry.getStats ("Derived from workspace data" badge). Policy ack assignment + reminders (P1 #4 done) — assignPolicy (idempotent, explicit users or all client users), listOverdueAcks/selectAcksDueForReminder/runPolicyAckReminders + policyAck.assign procedure + reminder scheduler (12h, ENABLE_POLICY_ACK_REMINDERS) + PolicyAcknowledgmentPanel rewrite. Framework coverage (P1 #11 done) — SOC 2 / PCI DSS v4 / HIPAA / SOX control manifests (frameworkSeed + applyFrameworkControls) + presentational FrameworkLibraryPanel on FrameworksDashboard. Risk heatmap: normalizeSummaryStatus unifies DB + UI treatment vocabularies. Tests 412 → 482 (30 files), coverage 100% on 5 targets, tsc 2044 → 2041 (0 new, 3 fixed in touched files).
- **Cycle 5** (007a11f): Collector connections + credential management + UI triggers (P0 #1 complete) — evidenceCollectorConnections lib (masked credentials, never expose secrets; DB-backed with in-memory fallback; provider manifests from the evidence registry; self-registering built-ins), evidenceCollectors tRPC router (listProviders/list/save/remove/test/run, zod-validated, TRPCError-wrapped), wired into routers.ts. UI: CollectorConnectionsPanel on Evidence page replaces the static strip — per-provider status chips, dynamic credential form from manifests (password/select/number/boolean fields), Test connection, Run-now trigger with live summary, delete with confirm, token-only per UI-STANDARD, dark-mode safe. Tests 374 → 412 (28 files), tsc: 0 new errors in touched files (Evidence/routers errors verified pre-existing).
- **Cycle 4** (b616e84): AWS/Azure/GCP evidence collectors (P0 #1) — 3 manifest-driven, injectable-fetch collectors (12 evidence types total: IAM key age / S3 encryption / EC2 public ports / CloudTrail; MFA / Defender / storage encryption / SQL auditing; bucket public access / SA key rotation / disk encryption / Cloud SQL SSL) registered in the evidence registry with graceful degradation + injected clock/TTL/limit. Per-client auto-test schedule config (P0 #2) — client_auto_test_schedules table + isAutoTestDue/getClientAutoTestSchedule/touchClientAutoTestRun/setClientAutoTestSchedule, scheduler skips disabled/not-due clients, controlMonitoring.getScheduleConfig/updateScheduleConfig (1-168h, zod). UI: Auto-test schedule panel on Controls (toggle + 1/6/12/24h + last-run) + Evidence-page "Automated evidence sources" strip, token-only per UI-STANDARD. Tests 271 → 374 (26 files), coverage 100% on 5 targets, tsc: 0 new errors in touched files (Controls/Evidence page errors verified pre-existing).
- **Cycle 3** (c3e6e68): Risk heat map + treatment plans (P1 #3) shipped - 5x5 likelihood x impact matrix, per-cell drill-down (listTreatmentPlans), treatment summaries + status transitions, RiskHeatmapPage route. Policy ack (P1 #4) - policy_acknowledgements schema + policyAck router (list/listPending/acknowledge-by-id/listForPolicy/summary) + PolicyAcknowledgmentPanel on ClientPoliciesPage. Real-time dashboard (P1 #12) - dashboardStats aggregation lib + dashboard.getStats (posture score, framework pass rates, evidence coverage, 30-day trend) + Vanta-style PostureSummary with enhanced-payload fallback. P0 #2 UI trigger - Control Auto-Tests section on Controls page (runAllForClient + history). Smoke fixed: Prowler mock option (no more live Docker scan with fake creds). Backend aligned to UI-STANDARD section 16 contract (riskHeatmap.listTreatmentPlans, policyAck.list/acknowledge, dashboard.getStats). Tests 192 -> 271 (18 files), coverage 100% on 5 targets, tsc no new errors in touched files.
- **Cycle 2** (c439fed): Control auto-test engine wired into tRPC (controlMonitoring router: runControlAutoTest / runAllForClient / history) + offline guard in scheduler (no more TLS spam to dead remote). Evidence collection scheduler + HTTP/API collector added (P0 #1 auto-collect). dbUrl helper (resolveDatabaseUrl/createSafePgOptions) used by addon-init + schedulers. UI token-only polish on Controls/Evidence/Login/Security/VendorList/ClientPolicies + PageHeader/StatCard components + primary-cta tokens. Tests 147 → 192 (12 files), coverage 100% on 5 targets. tsc: no new errors in touched files (2 dbUrl errors fixed).
- **Cycle 1** (78ee770): Local Supabase running (DB connected, health 200). AddonScheduler db.select + Date-binding bugs fixed. P0 evidence-collection integration framework skeleton + GitHub collector built (integrations/collector.ts). Vanta UI polish on Clients/Dashboard/NotFound/sidebar + UI-STANDARD.md. Tests 101 → 147 (9 files), all green.

| # | Feature | Vanta | ComplianceOS today | Priority | Status |
|---|---------|-------|--------------------|----------|--------|
| 1 | Automated evidence collection | 200+ integrations, 24h auto-collect | Framework + GitHub + HTTP/API + AWS + Azure + GCP collectors, scheduler wired (startEvidenceScheduler), per-connection credential management (masked, never exposed) + connection UI + test/run triggers (cycle 5) | P0 | 🟩 |
| 2 | Continuous control monitoring | Controls auto-tested on schedule | Engine wired via tRPC + UI trigger on Controls page + per-client schedule config (enable/interval, getScheduleConfig/updateScheduleConfig) | P0 | 🟩 |
| 3 | Risk heat map + treatment plans | Full visual matrix + workflow | 5x5 matrix + drill-down + treatment status workflow (cycle 3) | P1 | 🟩 |
| 4 | Policy management + ack | Templates, versioning, employee ack | Templates + ack workflow + employee assignment (assign to users/all client users, idempotent) + overdue reminders (3-day threshold, 12h scheduler) | P1 | 🟩 |
| 5 | TPRM / vendor risk | Vendor assessments, questionnaires, monitoring | Full TPRM suite + per-vendor risk tiering: vendor records + assessments (residual risk → vendor criticality + risk-register entries), contracts, DPAs, subprocessor register, SOC2 parser, 20-page tprm module (VendorList/Dashboard/Details, AssessmentTemplates, SecurityReviews, GlobalVendorCatalog, OnboardVendor, OverdueAssessmentsPage). Cycle 11 adds the vendorRisk engine (pure, deterministic per-vendor tier: data-access inherent risk + SOC2/scan/assessment/contract/DPA/subprocessor signals → residual score → Tier 1/2/3 + review cadence + next-review date + recommended actions), DB-backed vendorRisk router (getOverview/getVendorRisk, graceful degradation), vendorRiskApi contract layer, VendorDashboard risk overview + per-vendor tier surfacing on VendorList/VendorDetails (cycle 11) | P2 | 🟩 |
| 6 | Questionnaires & surveys | SOC2/ISO/CAIQ/SIG prebuilt, auto-scoring | Questionnaire router (17 procedures incl. score/scoreAnswers) + SIG Lite + CCM CAIQ v4 built-in templates + deterministic auto-scoring engine (readiness 0-100, focus-area breakdown, summarize) + gap-questionnaire email flows + public questionnaire route + vendor invite flow + Excel/JSON export + auto-answer/generateAnswers + per-questionnaire Score surfacing on Dashboard/Workspace (cycle 12) | P2 | 🟩 |
| 7 | Access review automation | Auto-provisioned reviews + certification | Cycles + certification tasks (auto-provision per user x role, idempotent), overdue sweep scheduler, certify/revoke workflow + audit history, AccessReviews page | P2 | 🟩 |
| 8 | Trust center | Public posture page | trustCenter router (public data, access request, NDA sign, access status) + TrustCenter page + vendor trust center; cycle 13: hardened graceful DB degradation + new public `getPosture` (compliance score 0-100 + Strong/Developing/At Risk/No Data status + implemented/total controls + documents), surfaced on the TrustCenter page with EmptyState/Skeleton degradation; contract layer trustCenterApi.ts | P2 | 🟩 |
| 9 | Audit management | Evidence packages, auditor portal | auditPackage router (ZIP audit package: PDF report + CSV + JSON manifests), auditor portal, AuditHub/AuditManager pages, board summary; cycle 13: generator hardened (pure helpers + structured AuditPackageError → TRPCError NOT_FOUND/INTERNAL_SERVER_ERROR), contract layer auditApi.ts | P2 | 🟩 |
| 10 | AI-powered compliance | Auto-map, suggest evidence, draft policies | In-app AI Copilot (cycle 14): deterministic built-in engine lib/ai/copilot.ts — draftPolicyDraft (10 topic templates + generic, orgName-aware, framework-specific controls), suggestEvidenceForControl (evidence catalog + freshness hints), autoMapRequirement (0-100 tiered keyword scoring over SOC 2 / ISO 27001 / NIS2 / GDPR / HIPAA / PCI DSS control KB, ranked matches + bestMatch) — never throws, zero deps; aiCopilot tRPC router (protected draftPolicy/suggestEvidence/autoMap + public status, zod BAD_REQUEST only); AgentPage Compliance Copilot quick-actions (draft/suggest/auto-map, token-only, Skeleton + EmptyState degradation); contract layer aiCopilotApi.ts. Plus existing: Hermes agent (packages/compliance-agent + docker/hermes-agent), agent chat page (/agent) with local fallback, LLM provider config router (llm), policy generation (/api/ai/generate-stream), lib/ai suggestion engines | P3 | 🟩 |
| 11 | Framework coverage | SOC2/ISO/HIPAA/PCI/GDPR/NIST/FedRAMP/SOX | NIS2, DORA, GDPR, ISO 27001:2022, NIST CSF + custom builder + SOC 2 / PCI DSS v4 / HIPAA / SOX manifests (frameworkSeed, catalog panel) | P1 | 🟩 |
| 12 | Real-time dashboard | Score, pass rate, coverage, trends, drill-down | dashboard.getStats + PostureSummary (score/coverage/trend) | P1 | 🟩 |
| 13 | Integrations & API | REST API, marketplace, webhooks, SSO | REST API (api-v1) + webhooks (cycle 8) + integrations router + addon registry + addon marketplace (pages/addons) + Enterprise SSO (cycle 9: OIDC auth-code flow w/ JWKS verification + reverse-proxy header auth) | P2 | 🟩 |
| 14 | Evidence expiration & renewal | Expiry tracking + auto-remediation | evidenceExpirationScheduler + renewal loop (getSummary/runNow, auto-remediation with remediation notes, UI panel) | P1 | 🟩 |
| 15 | Webhook system | Events out | Events out: registry w/ HMAC-SHA256 signatures, bounded retries/backoff (4xx not retried), subscription lifecycle (create/update/delete/enable-disable), event catalog (single source of truth), recursive secret scrubbing, delivery logs, graceful DB degradation; wired events (evidence.expired, control.autotest.failed, policy.ack.overdue, risk.created high/critical); Webhooks page + /webhooks route + nav (cycle 8) | P2 | 🟩 |

## Health (runtime)
- [x] App boots (Vite dev server, port 5173)
- [x] Unit suite green (1893 tests, 78 files)
- [x] Smoke test green (`npm run smoke`)
- [x] Local Supabase running (DB connected, health 200 on :3005, Studio on :54323)
- [x] AddonScheduler `db.select` + Date-binding bugs fixed
- [x] ControlAutoTestScheduler offline guard (no TLS spam; skips when no local DB)

## Definition of Done (per cycle)
1. `npx vitest run` green (no regressions)
2. `npx tsc -p packages/core/tsconfig.json --noEmit` clean for touched files
3. Scorecard status updated, commits on `dev`, changelog line added
